# Handoff

## 템플릿 Edition·달력 연도 동기화 구현 · 2026-09-07

- 원인: 라이브러리 `[설정]` 저장이 `template.metadata.edition`과 카드 레코드만 바꾸고 저장된 `project.settings.year` 및 페이지 날짜 좌표를 갱신하지 않았다.
- 수정: `template-year-synchronizer.js`를 추가하고 설정 저장·일반 저장·복제·사용자 설정 적용 경로를 연결했다.
- 동기화 대상: `settings.year`, `metadata.edition`, 월력 앞·뒷면 페이지 날짜, 연력 기준, 월별 이미지·문구·스타일 키, `book.events` 날짜.
- 보존 대상: 기존 페이지/개체/Master/override, 이미지 배치·맞춤값, AI 디자인 자산과 영구 자산 참조, 기존 저장 형식.
- GitHub: PR #15 병합, `main@2f81017b406761e8f9943e04e8817127fc8d5b1e`.
- 검증: 전체 build, Studio 300/300, Sprint 2 및 전체 패키지 검사 통과. Production 배포와 새 모듈 HTTP 200, 초기 화면 및 제품 콘솔 오류 없음 확인.
- 사용자 육안확인: 기존 저장 템플릿의 Edition을 임시 연도로 변경하고 편집 재진입 후 표지·연력·12개월 월력·미니월력·학사일정·현재/전체 미리보기·검토 PDF를 확인한다. 확인 후 원하는 운영 연도로 되돌려 새 버전으로 저장한다.

## Designer Studio 구조 개선 완료 · 2026-09-07

- 기준선 `main@99aaf67`에서 시작한 구조 개선을 작업 브랜치 `refactor/designer-structure-20260906`에서 완료했다.
- `apps/designer-studio/index.html`은 약 714KB에서 80,812 bytes로 축소됐고 인라인 JavaScript는 0개다.
- AI 디자인, 월력 렌더링, 개체 편집, 미리보기/PDF, 템플릿 설정/라이브러리를 소유 기능 모듈로 분리했다.
- v32~v38, RC4·RC5, Sprint 2, 서비스 Shell, 관리자 랜딩, 자동 맞춤, 템플릿 설정 초기화 호환 코드를 네 소유 기능의 10개 실행 단계로 정리했다.
- 전체 빌드, JavaScript 문법검사, Studio 회귀검사 297개가 통과했다.
- 로컬 로그인·편집·Supabase 원격 저장과 설정 저장·재열기, 현재/전체 미리보기를 실제 화면에서 확인했다.
- 설정에서 연도를 변경한 뒤 기존 편집 페이지 연도가 자동 변경되지 않는 동작은 구조 개선 이전 기능이며 후속 기능 개선 과제로 분리한다.
- 구조 개선 완료 커밋은 `05966cb`, 중복 미리보기 정리 커밋은 `5209760`이다.

## 2026-09-06 AI 생성 이미지 저장·재열기 Production 최종 성공

- Production에서 기존 AI 생성 템플릿의 `편집`과 `이 템플릿으로 새로 만들기`를 모두 최종 확인했다.
- 페이지 선택, AI 생성 이미지 표시, 초안 저장이 정상 동작한다. 이미지 요청은 `/api/template-assets?content=<asset-id>`로 처리되며 사용자 캡처에서 전체 요청이 HTTP 200이고 브라우저 `blob:` URL로 정상 변환되는 것을 확인했다.
- 반복 실패의 최종 원인은 공통 Runtime의 달력·이미지 개체 누락이 아니라, 과거 저장본에 남은 Supabase 비공개 Storage 서명 URL이었다. 서명 URL은 만료되고 브라우저가 외부 응답을 ORB로 차단하므로 재열기 때 이미지를 직접 복원할 수 없었다.
- 신규 저장본은 `acdl-asset://<id>` 영구 참조를 사용한다. 기존 저장본은 열 때 과거 서명 URL에서 `storage_path`를 추출하고, DB의 `template_assets` 레코드와 자산 ID를 찾아 동일 출처 인증 API로 내려받은 뒤 `blob:` URL로 표시한다. 다음 저장부터는 영구 참조로 자동 전환한다.
- 관련 API는 `ids`, `paths`, `content` 세 경로를 지원한다. 브라우저가 Supabase 서명 URL을 직접 요청하지 않는 것이 정상 기준이다.
- 최종 운영 커밋은 `3d50d94d838813c3a3bde472777b104e89207613` (`fix: migrate legacy signed template assets`)이다. 선행 수정은 `aeea6c98b990b14c9413a3d2083cd2ecbaf34a47`과 캐시 무효화 `a6c204caeee3f8646970f3924e6e14aa16f28a52`다.
- 전체 `npm run build`, Studio 회귀검사 293/293, Sprint 2 제품 검사, 인라인 스크립트 24개가 통과했다. Production에서도 `legacyStoragePath`와 `template-assets?paths`가 배포된 것을 확인했다.
- 회귀 확인 기준: 기존 템플릿 `편집` 및 `이 템플릿으로 새로 만들기`에서 이미지·페이지 이동·저장·검토용 PDF를 확인하고, Network에 외부 Supabase URL의 `ERR_BLOCKED_BY_ORB`가 없어야 한다.
- 다음 구조개선 우선순위: 약 714KB의 거대한 `apps/designer-studio/index.html` 실행 코드를 AI 디자인 생성·월력 렌더링·개체 편집·미리보기/PDF·템플릿 라이브러리 모듈로 단계적으로 분리한다. 화면 동작을 바꾸지 않으면서 저장·배포 속도와 수정 안정성을 높이는 작업이다.

## 2026-09-06 템플릿 라이브러리 저장본 복원 수정

- `편집`과 `이 템플릿으로 새로 만들기`의 문서 준비를 `template-project-loader.js`로 통일했다.
- 원격 AI 자산은 편집용 임시 주소와 브라우저 복구용 `acdl-asset://` 영구 참조를 분리한다.
- 페이지가 없거나 ID가 잘못됐거나 AI 탁상형 저장본이 28면이 아니면 편집 화면 진입을 차단한다. 따라서 페이지 이동과 검토용 PDF가 빈 프로젝트에서 실행되지 않는다.
- 불러오기 시작 시 기존 프로젝트를 먼저 지우지 않으며, 전체 복원·검사 성공 뒤에만 편집 프로젝트를 교체한다.
- 새 실행형 검사는 AI 자산 주소 재발급, 영구 참조 보존, 28면 유지, 빈 페이지 및 27면 저장본 차단을 확인한다.
- 전체 build, Studio 280개, Sprint 2 제품 검사, 인라인 스크립트 24개 통과.
- 다음 확인: Production에서 기존 AI 생성 템플릿의 `편집` → AI 배경 → 28면 이동 → 검토용 PDF, 이어서 `이 템플릿으로 새로 만들기`의 동일 항목 확인.

## 2026-09-06 AI 생성본 2차 품질 보완

- AI 배경과 편집 개체 분리, 28면 구성, 개체 선택은 실제 시안에서 정상 확인됐다.
- 표지 개체는 95% 안쪽 안전영역에 배치하고 학교 상징 제목·상징 이미지는 독립 영역으로 재구성했다.
- 일정 저장값은 바꾸지 않고 출력용 인덱스에서 일정명 내부의 동일 명칭만 제거한다.
- 프롬프트 `school-calendar-design@0.7.1`은 제본·천공·점선 루프와 제작 표시형 가장자리 무늬를 금지한다.

## 디자인 유형 기준 0.2.0

- 운영 테스트에서 `전체 페이지 디자인 적용이 완료되지 않았습니다. 누락 24면` 오류를 확인했다. 실제 AI 대표 세트를 만들 때 12개월 변주 목록이 누락돼 0개월을 완료로 기록한 것이 원인이었으며, 대표 세트의 월 목록 보존과 12개월·24자산 실재 검증을 추가했다.
- 템플릿 설정 창은 최대 1280×880px로 확대했고 설명 문구와 카드 글자를 키웠다. ⑧ 페이지별 시작 구성의 와이어프레임은 선택한 역할과 세부 옵션별 실제 관계를 구분해 표시한다.
- 2026-09-05 배포본은 메인 HTML이 원격 파일 처리 안전 크기를 넘어 뒤쪽이 손상되며 시작 화면 초기화가 중단됐다. 핵심 CSS를 별도 파일로 분리해 HTML을 약 680KB로 줄이고 `renderAIMonthlyExpansionState`를 안전한 여러 줄 함수로 변경했다. 시작월·앞/뒤 간지·달력 크기·배포 크기 회귀검사를 포함한 전체 Studio 검사가 통과한다.
- 현재 활성 카탈로그는 `school-calendar-design-types@0.2.0`, 설계 JSON은 `ai-design-spec.v1@0.2.0`이다.
- 생성 결과는 디자이너가 최종 완성하는 편집 가능한 시작 구성이다. AI 완성품 자동 제작으로 범위를 확장하지 않는다.
- 샘플 검토에서 확인된 핵심 보완은 월력 뒷면의 월별 변화다. 고정형·좌우 교대형·순환형과 달력 전체의 통일·색상·계절·교대·스토리 리듬을 별도 저장한다.
- 빈티지 기록 감성은 사진 기록형과 장식 계열·재질 조합으로 표현해 레이아웃 유형의 불필요한 증가를 막았다. 기존 6개 페이지 역할 및 0.1.0 자원은 호환을 위해 유지한다.
- ⑨ 화면은 디자인 유형 적용 참고 샘플 → 월력 뒷면 배치안 선택 → 월력 전체 생성·진행상태 → 템플릿 편집 화면 시작으로 분리했다. 편집 시작 단계는 추가 AI 생성·자동 재생성을 수행하지 않는다.
- `템플릿 편집 화면 시작`은 실제 적용 단계와 연결된 진행 팝업(진행률·현재 단계)을 표시한다. 편집 화면 렌더 성공 후 설정 창을 닫고, 실패하면 부분 변경을 되돌린 상태에서 오류와 다시 시도 버튼을 표시한다.
- 실제 생성 연결 버전은 AI 모듈 `1.4.0`, 프롬프트 `school-calendar-design@0.6.0`, 페이지 규칙 `page-rules@0.5.0`, 개체 스타일 `design-object-style-presets@0.2.0`이다.
- 설계 JSON의 변화 리듬·월력 뒷면 변화·장식 계열·재질이 생성 프롬프트와 12개월 편집 스타일에 반영된다. 뒷면은 고정·홀짝 좌우 반전·세 배치 순환으로 적용하며 디자이너 최종 조정이 필요한 시작 구성임을 기록한다.

## 2026-09-06 첫 실제 생성본 검토 보완

- AI 대표 디자인은 기본 1개만 생성한다. 생성 완료 즉시 표지·연력·학교 상징·월력·월력 뒷면·뒷표지의 확대 미리보기를 표시하고, 확인 후 `이 디자인으로 진행`을 눌러 선택한다.
- AI 단계의 중복 조건은 앞 단계 디자인 유형 설계서에서 자동 초기화한다. 새 프로젝트 또는 설계서 변경 시에만 이전 생성 상태를 지우므로 동일 조건의 생성 비용을 반복하지 않는다.
- AI 모듈 실행 기준은 `1.6.0`이다. 프롬프트 `0.7.0`과 페이지 규칙 `0.6.0`은 유지한다.
- 두 번째 검토 PDF는 약 7.2MB로 경량화가 정상 적용됐지만, AI 배경의 부모 자유 개체 레이어가 월력보다 위에 있어 12개월 월력만 희미하게 덮이는 문제가 있었다.
- AI 배경을 페이지 전용 `ai-background-layer(z-index:1)`로 분리해 월력(`z-index:5`)과 일반 편집 개체(`z-index:20`) 사이의 실제 합성 순서를 바로잡았다.
- 월력 앞면은 첫 클릭으로 Master 전체를 선택하고, 선택 후 날짜 클릭으로 날짜 편집에 들어가는 2단계 선택 규칙을 사용한다.
- 첨부된 첫 샘플 PDF는 28면·260×180mm·약 68MB였고, 각 면에 3056×2112 전체면 AI 이미지가 포함되어 이미지 페이로드가 대부분을 차지했다.
- AI 배경은 이제 `ai-design-background` 전용 최하단·잠금·비선택 레이어이며 포인터 입력을 받지 않는다. 기존 저장본도 열 때 동일 규칙으로 정규화한다.
- 프로젝트는 AI 이미지 본문을 `template.resources.aiDesignAssets`에 한 번만 두고 페이지는 `assetId`로 참조한다. 생성 세션·선택안·월별 자산은 초안 저장 시 메타데이터만 보존한다.
- 프롬프트/규칙 실행 기준은 모듈 `1.5.0`, `school-calendar-design@0.7.0`, `page-rules@0.6.0`이다. AI 이미지는 종이 질감과 희박한 가장자리 장식만 생성하며 편집 개체의 형상을 모사하지 않는다.
- 검토 PDF에서는 AI 배경만 최대 1400px JPEG 품질 0.72로 경량화하고 빈 이미지 프레임 안내는 숨긴다. 인쇄용 원본과 Published Package 경로는 그대로다.
- 다음 실제 확인은 Production에서 새 템플릿 생성 후 편집 개체 선택 반응, 저장·재열기, 28면 미리보기, 경량 검토 PDF의 육안 품질과 파일 크기를 확인하는 것이다.
- 전체 build와 Studio 260/260, Sprint 2 제품 검사, 인라인 스크립트 24개 통과.

## 2026-09-05 최신 인수인계

- 현재 최우선은 `⑧ 디자인 유형 설정`에서 시작하는 아래 10단계 로드맵이다. 사용자가 별도로 순서를 바꾸기 전까지 다른 작업 흐름으로 전환하지 않는다.
- 오늘 작업은 `AI 생성 기능 구현 → 실제 생성 초기 화면 검증에서 중단 → 디자인 다양성을 높이기 위한 디자인 유형 설정 기획·UI 초안` 순서로 진행됐다.
- AI 생성 기반은 `school-calendar-design@0.6.0`과 대표 디자인 세트 2안이다. 두 세트 모두 표지·연력·학교 상징·월력·월력 뒷면·뒷표지 6개 역할을 비교하며 역할별 디자인 스타일·페이지 유형·프롬프트 버전을 표시한다.
- 디자인 유형 1~10단계의 구현·자동 통합 검증이 완료됐고 전체 Studio 검사 259개, 인라인 검사 24개와 제품 연결 검사가 통과했다.
- `⑧ 디자인 유형 설정` 선택값은 설계 JSON 저장·복원, 편집 개체 배치, 개체 스타일, 역할별 프롬프트와 대표 2안 비교까지 연결됐다.
- 회사 OpenAI API 키는 ⑨단계에서 Master Admin이 직접 등록하며 Supabase Vault에 암호화 저장한다.
- 실제 등록 전에 `supabase/migrations/202609050001_ai_design_openai_vault.sql`을 해당 Supabase 프로젝트에 한 번 적용해야 한다.

### 고정 10단계

1. 디자인 유형 UI 완성: 페이지별 시작 구성, 연력·학교 상징·월력 와이어프레임, 핵심 개체·추천 용도·편집 주의 표시
2. 스타일·페이지 유형 카탈로그 버전 관리
3. 디자인 설계 JSON 생성·저장·복원
4. 유형 선택값을 실제 편집 개체 배치에 적용
5. 개체별 스타일 프리셋 연결
6. 설계 JSON을 6개 페이지 역할별 프롬프트에 연결
7. 대표 디자인 세트 2안 생성·비교
8. 선택 세트를 12개월·전체 페이지로 확장
9. 가독성·충돌·안전 영역·금지 텍스트·중복·일관성·해상도 자동 검사와 역할별 재생성
10. 저장·재열기·라이브러리·사용자 서비스·28면·PDF까지 통합 검증

### 새 채팅의 정확한 시작점

- 배포 화면에서 새 템플릿 1건의 생성·저장·재열기·28면 미리보기·검토용 PDF 육안 확인부터 계속한다.
- 디자인 유형은 완성품이 아니라 디자이너가 편집을 시작하는 구성 초안이다.
- 날짜·일정·학교 정보·교표·달력 격자는 편집 가능한 구조 개체로 유지하고 AI 이미지에 굽지 않는다.
- 10단계가 끝날 때까지 과거 검토 01·02, 별도 UI 정리, 사용자 서비스의 다른 기능으로 우선순위를 옮기지 않는다.
- 이미 통과한 244개/24개 검사는 회귀 징후나 관련 코드 변경이 없으면 새 채팅 시작 시 반복하지 않는다.

## 2026-09-03 이전 인수인계

- 템플릿 에디터 안정 기준: `main@2671761`, Vercel Production 배포 성공, 전체 Studio 테스트 189개 통과.
- 범용 달력 랜딩, Master Admin 이메일·비밀번호 로그인, Supabase `template_admins` 등록까지 완료했다.
- 다음 최우선 작업: [`[학사달력] 탁상형 검토 02 - 이미지 미니월력`](docs/product/05-DESK-REVIEW-02-COMPLETION.md)의 표지·연력·학교 상징 간지·월별 이미지/월력 12쌍·뒷표지 28면 완성.
- 샘플 3번 PDF에 최대한 가깝게 만들되 기존 Published Package와 검토 01은 변경하지 않는다.

## 다음 구현 단계 전에 개발자와 맞출 부분

1. 사용자 MVP가 편집기를 호출할 때 전달할 실행 파라미터
   - userId, schoolId, productId, templateId, projectId, returnUrl
2. 가격 산정 결과의 최종 권위
   - 화면 예상 금액과 우리학교인쇄 서버 확정 금액의 역할 분리
3. 최종 인쇄 파일 전달 방식
   - 업로드 API, 저장 위치, checksum, 재생성 정책, 보관 기간
4. 주문 상태와 제작 상태 매핑
   - 편집 중, 파일 생성, 검수, 주문 확정, 인쇄소 전달, 배송
5. 우리학교인쇄 운영 서버 배포 규칙
   - Node 버전, 빌드 명령, 정적 파일 또는 Next.js 서비스 방식, 환경변수, 로그/모니터링

## 자동으로 변경하지 않은 영역

- 다중 관리자 역할·초대·권한 관리
- 결제와 가격 확정 로직
- 개인정보 처리
- 운영 DB와 migration
- 우리학교인쇄 실제 API
- 인쇄소 전달 API
- 기존 UI와 디자인 토큰

## 템플릿 영구 저장 전환에서 맞출 부분

1. 개발·검토용 Supabase `calendar-editor-runtime-dev`에 `supabase/migrations/202608240001_template_persistence.sql` 적용 완료
2. Vercel 서버 환경에 `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` 등록 (`TEMPLATE_EDITOR_ACCESS_TOKEN`은 더 이상 사용하지 않음)
3. 서비스 키는 브라우저에 노출하지 않고 `/api/templates` 계열 서버 경로에서만 사용
4. `supabase/migrations/202609030001_template_master_admin.sql`을 적용하고 Supabase Auth에서 최초 사용자를 만든 뒤 `template_admins`에 `master_admin`으로 등록
5. 현재 단계는 단일 Master Admin 기준이며, 다중 사용자 초대·역할·권한은 이후 별도 설계
6. `template-assets`는 검토용 이미지 보관 범위이며 장기 보관·운영 자산 정책은 운영 전환 전에 재확인
7. 운영 전환 전 버전 보관 기간, 템플릿 삭제 방식, 이미지 미사용 자산 정리 정책 확정
8. 비공개 이미지 주소는 템플릿을 열 때마다 24시간 유효 주소로 다시 발급하며, 원격 프로젝트에는 `acdl-asset://<id>` 참조만 저장
