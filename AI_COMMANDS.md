# AI Commands

학교사업팀 팀원이 AI에게 말하는 공식 요청입니다. 저장/불러오기는 GitHub 기준입니다.

팀원에게는 아래 표의 한국어 문장을 그대로 말하도록 안내합니다. 같은 요청을 `/schoolp-*` 슬래시로도 실행할 수 있으며, Claude Code·Codex에서 동일하게 동작합니다.

AI는 새 세션을 시작하면 먼저 이 표를 보여줍니다. 팀원에게 문제 해결용 내부 명령을 따로 외우게 하지 않습니다.

| 하고 싶은 일 | 이렇게 말하세요 |
|---|---|
| 로컬에서 화면 보기 | "실행해줘" |
| GitHub에 작업 저장하기 | "작업 저장해줘" |
| GitHub에서 불러오기 | "작업 불러와줘" |
| 팀 공유용으로 배포하기 | "배포해줘" |
| 핸드오프 정리하기 | "핸드오프 정리해줘" |
| 간단한 사용 안내 보기 | "도움말" |

표 아래에는 짧은 사용 안내를 덧붙입니다. 새 화면이나 기능은 만들 내용을 그대로 설명하면 되고, 막히거나 궁금한 점은 상황을 그대로 질문하면 됩니다. 점검과 최신 기준 확인은 필요한 시점에 AI가 처리하고, Supabase 데이터 확인은 요청하면 됩니다. 표에는 슬래시를 노출하지 않고 "슬래시로도 됩니다" 정도만 짧게 덧붙입니다. 슬래시 매핑(AI 참고용): 실행=`/schoolp-run` · 작업 저장=`/schoolp-save` · 불러오기=`/schoolp-load` · 배포=`/schoolp-deploy` · 핸드오프=`/schoolp-handoff` · 도움말=`/schoolp-help`.

이 6개가 팀원에게 노출하는 공식 요청입니다. 나머지는 표에 넣지 않고, 필요할 때 말로 부르거나 AI가 알아서 처리합니다:

- **기획 브레인스토밍**(`/schoolp-brainstorm`) — 팀원이 새 프로젝트·화면·기능을 설명하면 AI가 자동으로 요구사항 정리부터 시작합니다. 명령으로 표에 넣지 않습니다(직접 부르려면 "기획해줘").
- **점검**("점검해줘" / `/schoolp-verify`) — 보통 저장·배포 안에서 자동으로 실행됩니다.
- **최신 기준 확인**("최신 기준 확인해줘" / `/schoolp-update`) — 세션 시작 시 자동으로 감지됩니다.
- **Supabase 열기**("Supabase 열어줘" / `/schoolp-supabase`) — DB 데이터를 직접 볼 때만.
- **이어하기 메모**(`WORK_CONTEXT.md`) — "작업 저장해줘" 때 자동으로 갱신되고 "불러와줘" 때 자동으로 읽어 브리핑합니다. 따로 부를 일은 드뭅니다.

### 커맨드는 어떻게 동작하나

- 위 커맨드는 스타터의 **스킬**로 들어 있습니다. Claude Code는 `.claude/skills/`, Codex는 `.agents/skills/`에서 같은 스킬을 읽습니다(내용 동일). 두 폴더 모두 프로젝트에 포함되어 팀원 모두가 공유합니다.
- **한국어 문장으로 말하거나 `/schoolp-*` 슬래시로 부르면** AI가 해당 스킬을 실행합니다. Claude Code·Codex 공통이며, 슬래시를 외우지 못해도 됩니다.
- `기획해줘`(`/schoolp-brainstorm`)는 팀원이 직접 부를 수도 있고, 새 기능·화면 요청이 들어오면 AI가 먼저 자동으로 시작하기도 합니다. 절차는 `docs/PLANNING_ELICITATION.md`.

GitHub 저장은 회사/집/다른 PC에서 이어서 작업하기 위한 것입니다.
Vercel production alias는 팀 공유용 화면 검토 링크입니다. 사용자 공개 반영이나 서비스 오픈이 아닙니다.

`도움말` 또는 `커맨드 목록 보여줘`라고 말하면 위 공식 요청 표와 짧은 사용 안내를 보여줍니다(`/schoolp-help`).

AI는 짧은 요청을 받더라도 `AI_START_HERE.md`, `STARTER_VERSION.md`, `CHANGELOG.md`, `WORK_LOG.md`, `README.md`의 기준을 먼저 확인합니다.

중요: `저장해줘`만 단독으로 입력되면 바로 GitHub 저장, 문서 저장, 파일 수정을 진행하지 않습니다. AI는 `GitHub에 저장하려면 작업 저장해줘라고 말해 주세요. 지금 GitHub에 저장할까요?`처럼 짧게 확인합니다.

## 실행해줘

로컬 개발 서버를 실행합니다.

같이 인식할 표현:

- 시작해줘
- 켜줘
- 로컬에서 보여줘

기준:

- 기본 명령: `npm run dev`
- 로컬 주소: `http://localhost:3000`
- 다른 포트로 우회 실행 금지
- 3000번 포트가 사용 중이면 기존 dev server 종료 후 재실행
- Windows에서는 `start-windows.cmd` 우선 사용

## 작업 저장해줘

현재 작업 상태를 GitHub에 저장합니다.

같이 인식할 표현:

- 커밋해줘
- GitHub에 저장해줘
- GitHub에 업로드해줘

기준:

- GitHub는 작업 저장과 이어서 작업하기 위한 용도입니다.
- 사용자 공개 반영이나 서비스 오픈이 아닙니다.
- 팀원은 Git 명령을 직접 실행하지 않고, AI가 정해진 안전 절차 안에서만 `git`과 `gh`를 사용합니다.
- Git 저장소가 아니거나 GitHub CLI가 없어도 실패로 끝내지 않고, AI가 가능한 준비 절차를 먼저 진행합니다.
- 처음 저장하는 프로젝트는 AI가 `git init`, GitHub CLI 준비, 로그인 확인 후 private 저장소를 생성합니다.
- 저장소는 반드시 private으로 만듭니다.
- `.env*`, DB 파일, `.vercel`, `.next`, `node_modules`, ZIP, 로그 파일은 저장하지 않습니다. 단, `.env.example`은 저장할 수 있습니다.
- 저장 전 `WORK_LOG.md`를 현재 작업 맥락으로 갱신합니다.
- 저장 전 `git status`로 변경 파일을 확인합니다.
- 가능하면 `npm run style:check`, `npm run build`를 실행합니다.
- 저장하면서 `WORK_CONTEXT.md`(이어하기 메모)도 지금 상태 기준으로 자동 갱신합니다 — 오늘 한 일/현재 상태/다음 할 일. 팀원이 따로 요청하지 않아도 됩니다.
- 저장 후 GitHub 저장소 URL, branch, commit hash를 알려줍니다.

처음 저장할 때 인증이 필요하면 AI가 로그인 절차를 시작하고, 팀원이 본인 GitHub 계정으로 브라우저 로그인/회원가입/2FA를 진행하게 안내합니다. 인증이 끝나면 AI가 저장을 계속합니다.

## 최신 작업 불러와줘

GitHub에 저장된 최신 작업을 현재 PC로 가져옵니다.

같이 인식할 표현:

- 최신 작업 받아줘
- 이어서 작업하게 불러와줘
- GitHub에서 불러와줘

기준:

- GitHub CLI가 없으면 가능한 설치/준비 절차를 먼저 진행합니다.
- GitHub CLI 로그인 상태를 확인합니다.
- 현재 폴더에 저장하지 않은 변경이 있으면 먼저 `작업 저장해줘`를 제안합니다.
- remote 저장소가 있는지 확인합니다.
- `git fetch`로 최신 상태를 확인한 뒤, fast-forward 가능한 경우에만 `git pull --ff-only`로 가져옵니다.
- 충돌, divergent branch, 저장하지 않은 변경사항이 있으면 자동으로 해결하지 않고 상황을 설명합니다.
- 가져온 뒤 `npm install` 필요 여부와 실행 상태를 안내합니다.
- 가져온 뒤 `WORK_CONTEXT.md`(이어하기 메모)가 있으면 먼저 읽고 "지난번 여기까지 했고, 다음은 이거예요"로 짧게 브리핑한 뒤 이어서 진행할지 확인합니다.

## GitHub 준비 자동화

`작업 저장해줘` 또는 `최신 작업 불러와줘` 요청에서 GitHub 준비가 안 되어 있어도 그 이유만 말하고 멈추지 않습니다. AI는 가능한 준비 작업을 순서대로 진행합니다.

1. `git --version`으로 Git 설치 여부를 확인합니다.
2. Git 저장소가 아니면 `git init`을 진행합니다.
3. `gh --version`으로 GitHub CLI 설치 여부를 확인합니다.
4. `gh`가 없으면 OS에 맞게 설치를 시도합니다.
   - macOS: Homebrew가 있으면 `brew install gh`를 시도합니다.
   - Windows: `winget`이 있으면 `winget install --id GitHub.cli -e`를 시도합니다.
   - 위 방법이 없거나 관리자 권한이 필요하면 GitHub CLI 공식 설치 페이지를 안내하고, 설치 후 Codex/터미널을 다시 열게 합니다.
5. `gh auth status`로 로그인 상태를 확인합니다.
6. 로그인이 안 되어 있으면 `gh auth login`을 실행하고, 브라우저 로그인/회원가입/2FA는 팀원이 직접 진행하게 안내합니다.
7. 인증이 끝나면 AI가 다시 상태를 확인하고 private 저장소 생성, remote 연결, commit, push를 계속 진행합니다.

회원가입, 브라우저 로그인, 비밀번호 입력, 2FA 입력은 AI가 대신하지 않습니다. 하지만 인증이 필요한 지점까지는 AI가 직접 진행하고, 인증 완료 후에는 저장 절차를 계속합니다.

인증 URL이 표시되면 AI는 가능한 경우 브라우저를 열어 해당 URL로 이동합니다. 동시에 팀원이 직접 열 수 있도록 클릭 가능한 링크와 코드를 함께 보여줍니다.

예시:

```text
GitHub 인증이 필요합니다.
URL: [GitHub 인증 페이지](https://github.com/login/device)
Code: ABCD-1234
브라우저에서 로그인/회원가입/2FA/Authorize를 완료해 주세요. 인증이 끝나면 제가 이어서 진행합니다.
```

브라우저 제어가 불가능하면 URL과 코드를 복사하기 쉽게 위 형식으로 안내합니다. 로그인, 회원가입, 비밀번호 입력, 2FA, 계정 선택, 권한 승인은 팀원이 직접 진행합니다.

## 배포해줘

현재 작업을 Vercel에 배포합니다.

같이 인식할 표현:

- 현재 작업 배포해줘
- 저장하고 배포해줘
- 배포만 해줘

기준:

- Vercel 배포는 화면 확인용입니다.
- Vercel production alias를 팀 공유용 화면 검토 링크로 사용합니다. 사용자 공개 반영이나 서비스 오픈은 아닙니다.
- 배포 전 AI가 내부적으로 starter required 기준 누락 여부를 확인합니다.
- 배포 전 `npm run build`를 실행합니다.
- 기본 배포 명령: `npm run deploy`
- Vercel CLI, 로그인, project link가 없어도 실패로 끝내지 않고, AI가 가능한 준비 절차를 먼저 진행합니다.
- 첫 배포 또는 프로젝트 연결이 불확실하면 Vercel 로그인 계정, team, project를 확인하고 짧게 보고합니다.
- 다른 배포 플랫폼으로 우회하지 않습니다.
- 처음 배포하는 PC에서는 AI가 로그인 절차를 시작하고, 팀원이 본인 Vercel 계정으로 브라우저 로그인/회원가입/2FA를 진행합니다. 인증이 끝나면 AI가 배포를 계속합니다.
- 공식 도메인, 운영 환경변수 설정, DB 전환, 사용자 공개 반영은 개발자 확인 후 진행합니다.

배포 전 저장 여부:

- 저장하지 않은 변경사항이 있으면 먼저 아래처럼 짧게 묻습니다.

```text
배포 전에 현재 작업을 GitHub에 저장할까요?

1. 저장 후 배포하기 (추천)
2. 배포만 하기
```

- 사용자가 `저장하고 배포해줘`라고 말하면 질문 없이 저장 후 배포합니다.
- 사용자가 `배포만 해줘`라고 말하면 저장하지 않고 배포만 합니다.
- 저장하지 않고 배포한 경우, 완료 보고에 `GitHub 미저장 배포`, Vercel URL, 마지막 Git commit 또는 `없음`, 빌드 확인 시각을 표시합니다.

## Vercel 준비 자동화

`배포해줘` 요청에서 Vercel 준비가 안 되어 있어도 그 이유만 말하고 멈추지 않습니다. AI는 가능한 준비 작업을 순서대로 진행합니다.

1. `node --version`, `npm --version`으로 Node/npm 사용 가능 여부를 확인합니다.
2. Vercel CLI가 없으면 우선 `npx vercel` 또는 `npx vercel@latest` 기준으로 진행합니다.
3. Vercel 로그인이 필요하면 CLI가 안내하는 브라우저 인증을 팀원이 직접 진행하게 합니다.
4. Vercel 계정이 없으면 팀원이 브라우저에서 회원가입을 진행하게 안내합니다.
5. project link가 없으면 AI가 `vercel link` 흐름을 진행하고, 선택이 필요한 항목은 팀원에게 짧게 확인합니다.
6. 인증과 연결이 끝나면 AI가 다시 빌드 확인 후 production alias 배포를 계속합니다.

회원가입, 브라우저 로그인, 비밀번호 입력, 2FA 입력은 AI가 대신하지 않습니다. 하지만 CLI 준비, 로그인 시작, project link, 배포 재시도는 AI가 이어서 진행합니다.

인증 URL이 표시되면 AI는 가능한 경우 브라우저를 열어 해당 URL로 이동합니다. 동시에 팀원이 직접 열 수 있도록 클릭 가능한 링크를 함께 보여줍니다.

예시:

```text
Vercel 인증이 필요합니다.
URL: [Vercel 인증 페이지](https://...)
브라우저에서 로그인/회원가입/이메일 인증/2FA를 완료해 주세요. 인증이 끝나면 제가 이어서 배포합니다.
```

브라우저 제어가 불가능하면 URL을 복사하기 쉽게 위 형식으로 안내합니다. 로그인, 회원가입, 비밀번호 입력, 이메일 인증, 2FA, 계정 선택, 권한 승인은 팀원이 직접 진행합니다.

## 점검해줘

현재 작업이 계속 개발·배포 가능한 상태인지 확인합니다.

같이 인식할 표현:

- 검증해줘
- 문제 없는지 봐줘
- 배포 전 확인해줘

기준:

- 기본 확인: `npm run style:check`, `npm run build`
- 가능하면 `npm run lint`, `npm run typecheck`도 실행합니다.
- `/api/*`, `src/lib/api/contracts.ts`, Supabase migration/seed, env key 누락, 금지 파일 포함 여부를 확인합니다.
- 실패하면 같은 명령을 반복하지 않고 원인을 분류합니다.

## 최신 기준 확인해줘

schoolp starter 최신 기준을 확인하거나 안전하게 반영합니다.

같이 인식할 표현:

- 스타터 업데이트 해줘
- 기준 반영해줘
- 최신 기준 반영해줘

기준:

- 확인만 요청받으면 `npm run schoolp:update:check`로 차이만 보고하고 파일은 수정하지 않습니다.
- 반영 요청이면 `docs/UPDATE_APPLY_GUIDE.md` 절차로 기존 화면·기능·스타일을 보존하며 부족한 기준만 반영합니다.
- `.claude/skills`, `.agents/skills`, hooks, scripts, docs, manifest처럼 기준 파일 중심으로 보강합니다.

## Supabase 열어줘

개발/검토용 Supabase 데이터를 웹에서 확인합니다.

같이 인식할 표현:

- DB 데이터 보여줘
- Supabase 데이터 확인해줘
- Supabase 테이블 열어줘

기준:

- Supabase Dashboard의 Table Editor에서 데이터를 확인·입력·수정할 수 있습니다.
- 팀원이 브라우저에서 직접 로그인/회원가입/2FA/project 선택을 진행합니다.
- AI는 secret, service role key, access token 값을 채팅에 요구하거나 출력하지 않습니다.
- 운영 DB가 아니라 개발/검토용 Supabase 데이터라고 표현합니다.

## 핸드오프 정리해줘

만든 프로토타입을 실제 서버·DB로 연결하도록, 개발자가 검토할 계약·API·DB·env·운영 전환 항목을 인수인계(핸드오프) 문서로 정리합니다. 이 스타터에서 "핸드오프"는 개발자에게 넘기는 이 문서를 뜻합니다.

같이 인식할 표현:

- 핸드오프 문서 만들어줘
- 개발자에게 넘길 내용 정리해줘
- 개발 전달용으로 정리해줘

기준:

- `HANDOFF.md` 양식을 기준으로 정리합니다.
- 화면, `/api/*` route, `src/lib/api/contracts.ts`, Supabase migration/seed, env key, mock/real 연동 상태를 확인합니다.
- secret 값은 출력하지 않습니다. 필요한 key 이름과 전달받아야 할 값만 적습니다.
- 운영 DB 종류, 공식 도메인, 사용자 공개 반영은 확정하지 않고 개발자 확인 항목으로 남깁니다.

## 이어하기 메모 (보통 자동)

다음 세션(다른 PC, 새 세션, 다른 팀원 포함)이 지금 상태를 바로 이어받을 수 있도록 오늘 한 일/현재 상태/다음 할 일을 `WORK_CONTEXT.md`에 남깁니다. **보통은 팀원이 따로 부르지 않아도 됩니다** — "작업 저장해줘" 때 자동으로 갱신되고, "불러와줘" 때 자동으로 읽어 브리핑합니다.

같이 인식할 표현(직접 부를 때):

- 다음 세션 메모 남겨줘
- 오늘 작업 정리해줘
- 이어받게 메모 남겨줘

기준:

- `WORK_CONTEXT.md`를 지금 상태로 새로 써서 덮어씁니다. 이전 회차 기록은 남기지 않습니다.
- 히스토리가 필요하면 `WORK_LOG.md`, `CHANGELOG.md`를 참고하도록 안내합니다. 이 문서에는 쌓지 않습니다.
- 실제 연동·운영 전환·확인이 필요한 항목은 이 문서 대신 핸드오프 문서("핸드오프 정리해줘", `/schoolp-handoff`)로 안내합니다.
- GitHub 저장이 필요하면 "작업 저장해줘"(`/schoolp-save`)를 별도로 안내합니다.

## AI 내부 절차

아래 작업은 팀원이 외워서 말하는 커맨드가 아닙니다. AI가 위 공식 요청을 수행하면서 필요한 경우 내부적으로 처리합니다.

- 스타일/Tailwind 연결 점검
- `WORK_LOG.md` 작업 맥락 갱신
- 빌드 오류 원인 분류

팀원이 위 내용을 직접 요청하더라도, AI는 별도 커맨드처럼 늘어놓지 않고 현재 상황에 맞춰 위 공식 요청 중 무엇에 해당하는지 안내합니다.

## 최신 기준 자동 확인 (SessionStart 훅)

세션이 시작되면 SessionStart 훅이 `node scripts/schoolp-update-check.mjs --hook`을 실행해, 원격 manifest의 최신 기준과 현재 프로젝트 기준을 자동으로 비교합니다.

- 설정 위치: Claude Code는 `.claude/settings.json`, Codex는 `.codex/hooks.json`. 둘 다 같은 스크립트를 부릅니다.
- 최신이면 아무것도 하지 않습니다. 오래됐으면 안내가 대화에 주입됩니다.
- 그러면 AI는 **첫 응답 맨 앞에서, 팀원의 원래 요청을 처리하기 전에** 예/아니오로 먼저 묻습니다: "스타터 업데이트가 있어요(rXX → rYY). 지금 반영할까요? 기존 화면·기능은 그대로, 부족한 기준만 안전하게 맞춥니다. 1) 네, 반영하고 진행 2) 나중에".
- "네"면 `docs/UPDATE_APPLY_GUIDE.md` 절차로 안전 반영(앱 보존, `.claude/skills`·`.agents/skills`·설정·scripts 복사, 세션 재시작 안내) 후 원래 요청을 이어서 처리하고 무엇을 바꿨는지 한 줄로 보고합니다.
- "나중에"면 반영하지 않고 원래 요청만 처리하며 이번 세션에서는 다시 묻지 않습니다.
- 네트워크가 안 되면 조용히 넘어갑니다(세션을 막지 않음).
