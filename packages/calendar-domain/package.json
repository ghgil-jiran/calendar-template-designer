{
  "name": "@calendar-publishing/calendar-domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node tests/calendar-domain.test.mjs"
  }
}
