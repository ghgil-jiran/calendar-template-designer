import assert from "node:assert/strict";import{buildTwelveMonths,validateSettings}from"../dist/index.js";const m=buildTwelveMonths(2027,8);assert.equal(m.length,12);assert.equal(m[0].key,"2027-08");assert.equal(m[5].key,"2028-01");assert.deepEqual(validateSettings({year:2027,startMonth:8,calendarType:"desk",weekStart:"sunday",gridRows:6,frontDividerCount:0,backDividerCount:0}),[]);console.log("calendar-domain ok");
