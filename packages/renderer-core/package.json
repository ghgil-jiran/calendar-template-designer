{
  "name": "@calendar-publishing/renderer-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node tests/renderer.test.mjs"
  }
}
