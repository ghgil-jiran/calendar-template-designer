# Work Context

## 현재 기준 · 2026-09-12

- 저장소: `ghgil-jiran/calendar-template-designer`
- 기준 브랜치: `main`
- 기준 커밋: `600d628ed28ec97b04d8ff4da30cfa8ac5ccad91`
- Production: `https://calendar-template-designer.vercel.app/`
- Preview의 페이지 구성·AI 디자인 개선을 `main`의 기존 변경과 병합했고, 전체 빌드와 Studio 회귀검사 430개가 통과했다.
- 기존 Published Package와 저장 템플릿은 자동 변경하거나 교체하지 않는다.
- Supabase 운영 데이터 변경은 없다.

## 완료한 큰 단계

- 페이지 설정을 물리 면과 실제 개체 구성의 단일 기준으로 정리했다. 연력은 페이지 역할이 아니라 표지 안쪽면·간지·뒷표지 안쪽면에 배치할 수 있는 개체로 취급한다.
- 26면형은 표지 안쪽면과 첫 월력 뒷면을 함께 사용하며, 별도 안쪽면을 사용하면 28면 이상으로 구성한다. 간지 앞·뒷면은 실제 선택 매수에 따라 동적으로 반영한다.
- 표지, 학교 소개·학교 상징·학사일정·Yearly Plan 간지, 월력 앞면·뒷면, 뒷표지의 대표 배치와 편집 개체 적용을 정리했다.
- 월력 뒷면은 대형 이미지·미니월력형, 월간 계획표 중심형, 이미지·메모 복합형, 사진 콜라주·띠력형을 제공하며 월력 띠력은 페이지 하단에 고정한다.
- AI 디자인 설정은 공통 지침, 스타일별 규칙, 실제 페이지별 구성·배치·금지 요소를 결합한다. 학교 건물과 학생을 포함한 사람은 AI 배경에서 생성하지 않고 실제 사진·교표·학교 정보는 교체 가능한 편집 개체로 유지한다.
- AI 생성 실패 후 재시도, 대표 자산 누락 복구, 전체 물리 면 적용 검사와 편집 화면 진입을 보정했다.
- 상업용 학사달력 기준으로 AI 장식 밀도를 낮추고 달력·일정·학교 정보 등 핵심 개체의 가독성과 안전영역을 우선하도록 조정했다.
- 표지의 빈 이미지 안내 제거, 연도 크기·위치, 교표 비율 유지, 월력 격자 상향 배치, 가로 사진 중앙 정렬, 뒷표지 연도·교표 중심 배치를 보정했다.

## 현재 단계

템플릿 에디터에서 완성한 템플릿을 라이브러리에 등록하고, 사용자 서비스가 게시된 템플릿을 조회·선택할 수 있도록 연결한다.

1. 사용자 서비스에 노출할 대표 탁상형 템플릿을 선택한다.
2. 템플릿 상태를 `published`로 확정하고 Template Package의 ID, 버전, SHA, 달력 유형을 고정한다.
3. 사용자 서비스의 템플릿 목록을 Template Registry의 게시 템플릿과 연결한다.
4. 사용자 서비스에서 템플릿 선택 후 학교 정보·학사일정·월별 이미지를 적용해 미리보기와 저장·재열기를 확인한다.
5. 같은 `ResolvedDocument`를 기준으로 화면 미리보기와 인쇄용 PDF 연결을 검증한다.

## 보존 원칙

- 사용자 로컬의 미커밋 변경과 `tmp/`를 폐기하거나 임의로 커밋하지 않는다.
- 표준이 아닌 템플릿은 원본 편집이 가능하지만, 표준 템플릿은 복제 후 새 초안으로 수정한다.
- 사용자 서비스에는 `published` 상태의 템플릿만 노출하며 `isStandard`는 노출 조건으로 사용하지 않는다.
- 학교 정보, 달력, 일정, 교표, 사진 프레임은 AI 이미지에 굽지 않고 편집 개체로 유지한다.
- 운영 DB·인증·결제·공식 도메인·인쇄 서버 전환은 별도 확인 없이 변경하지 않는다.
