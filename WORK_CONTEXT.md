# Work Context

## 현재 상태 · 2026-09-03

- 작업 대상은 `ghgil-jiran/calendar-template-designer`의 `main`이며 현재 원격 기준은 `694eb75`다.
- 월력 공통 계약 `monthly-calendar-preset.v1`과 기존 `sample-3/sample-6` 호환 계층이 Runtime·Contracts에 저장됐다.
- 샘플 6은 `academic-boxed@1.0.0`, 샘플 3은 `segmented-underline@1.0.0`으로 분리되며 둘 다 5행·6행을 지원한다.
- 샘플 6 원본 PDF의 벡터 치수를 측정해 Studio의 독립 프리셋 카탈로그와 CSS 변수 연결을 로컬 구현했다.
- 새 계약 문서와 기존 샘플 6 문서는 같은 카탈로그를 사용하며, 샘플 3은 박스형 카탈로그로 해석하지 않는다.
- 이번 로컬 변경은 전체 build와 Studio 검사 180개를 통과했으며 아직 GitHub에 저장하지 않았다.
- Inspector의 요일·날짜 격자 선택이 프리셋 기본값에 가려지던 회귀는 로컬에서 수정됐고, 기존 저장값과 새 `calendarOverrides`를 모두 지원한다.
- 열린 가로줄의 요일 바로 아래 경계 누락도 첫 날짜 행 전용 상단선으로 로컬 수정됐다.
- 월 제목과 요일 사이에는 원본 측정값 1.7mm의 프리셋 내부 여백을 적용하며 날짜 격자 높이는 유지한다.
- 날짜 격자는 `전체 박스`, 첨부 디자인 1 기반 `독립 밑줄`, 첨부 디자인 2 기반 `미니멀 무선`, `개별 사각 셀` 네 가지 표현을 Inspector에서 선택할 수 있다.
- 네 표현은 같은 날짜 계산·일정 데이터·5행·6행 구조를 사용하며, 첨부 디자인 2의 삽화·페이지 편집 구성은 날짜 격자 옵션의 범위에 포함하지 않는다.
- 월 표시 6종은 표현별 최소 제목 높이(10~18%)를 사용해 큰 월 숫자가 연도·영문월 또는 요일을 침범하지 않으며, 기존 한글형 저장값도 호환한다.
- 재검토 결과 큰 숫자형 최소 제목 높이는 18~20%로 상향했으며 월 표시 위치는 왼쪽·가운데·오른쪽을 지원한다.
- 개별 사각 셀의 기간 일정은 하나의 일정 데이터와 라벨을 유지하되 셀 사이 6px 간격에서는 색상 막대가 끊어져 카드 구조를 침범하지 않는다.
- 편집 화면은 중복된 `편집·삽입·정렬·보기` 메뉴 행을 제거했으며, 정렬은 도구상자, 현재·전체 미리보기는 상단 `미리 보기` 드롭다운에서 제공한다.
- 하단 페이지 구성은 112px의 간결한 가로 탐색 스트립이며 면 번호·역할·현재/전체 위치만 표시한다.
- 샘플 3 인쇄 PDF(260×180mm, 28면)를 기준으로 별도 `[학사달력] 탁상형 검토 02 - 이미지 미니월력` 초안을 로컬 구현했다.
- 샘플 3 초안은 6번 및 Published Package와 분리되며, 독립 요일 캡슐·세로선 없는 날짜 격자·3분할 월별 이미지·현재/전후월 미니월력과 표지·연력·학교상징·뒷표지 전용 면을 가진다.
- 이번 로컬 변경은 전체 build, Studio 검사 184개, 인라인 스크립트 21개를 통과했으며 아직 GitHub 저장·배포 전이다.

## 다음 할 일

1. 샘플 3 검토 02 초안을 Studio에서 표지·연력·학교상징·3월 뒷면·3월 월력·뒷표지 순으로 육안 확인한다.
2. 사용자 확인 후 GitHub `main`에 저장하고 Vercel Production에 배포한다.
3. 실제 월별 이미지가 입력된 상태에서 3분할 focal point와 전후월 미니월력 크기를 미세 조정한다.

## 이전 안정 상태 · 2026-09-02

- 작업 대상은 `ghgil-jiran/calendar-template-designer`의 `main`이며 탁상형 표준 01 문서 버전은 v13이다.
- 연력은 기본적으로 월력 행 수를 따르고, 개체 스타일에서 월력 설정 상속·월별 자동 5/6주·항상 5주·항상 6주를 선택할 수 있다.
- 템플릿 기본 설정의 5×7·6×7 선택은 페이지 구조에 적용되며 저장 후 고정 행 모드로 유지된다.
- 6번 템플릿에서는 3번 빠른 디자인 조합을 선택할 수 없다. 이전 작업본에 3번 조합이 저장돼 있으면 v13 migration이 6번 원본형 박스 격자와 넓은 세로 영역으로 복구한다.
- 빈 날짜 셀 미니 월력은 마지막 주에 연속 빈 칸 2개가 있을 때만 표시하고, 대상 월의 실제 5/6주 구조를 사용한다.
- 월력의 상단·왼쪽 선을 보강했으며 템플릿 설정 창이 열리면 도련·안전선은 잠시 숨긴다.
- 전체 build와 Studio 검사 166개, 에디터 인라인 스크립트 21개가 통과했다.
- 첨부 기준 자료는 탁상형 6번 월목표·할 일 플래너 PDF와 3번 이미지·월력·미니달력 PDF다.
- 템플릿 카드 이름은 `[학사달력] 달력유형 표준|검토 NN - 특징` 규칙을 사용하며 내부 Package 버전은 별도로 표시한다.
- 템플릿 에디터의 활성 카드는 `[학사달력] 탁상형 검토 01 - 월별 플래너`, `[학사달력] 탁상형 표준 02 - 월별 이미지`, `[학사달력] 벽걸이형 검토 01 - 이미지 월력`으로 정리됐다.

## 이전 확인 항목

1. Production에서 기존 표준 01 작업본을 다시 열어 v13 migration과 6번 원본형 복구를 확인한다.
2. 템플릿 설정에서 5×7과 6×7을 각각 저장해 월력 전체와 연력 상속 결과를 확인한다.
3. 연력 스타일의 네 가지 행 수 선택과 설정 창의 도련선 숨김을 확인한다.
4. 템플릿 에디터와 사용자 서비스의 카탈로그·자료수집 카드에서 상태·이름·내부 버전·면 구성이 같은지 나란히 확인한다.
5. 사용자 서비스 `integration/runtime-v2`에서 갱신된 템플릿을 선택해 28면 미리보기·저장·재열기 결과를 확인한다.

## 보존 원칙

- 기존 저장 원본은 불러오는 순간 덮어쓰지 않고 사용자가 저장할 때만 v13으로 기록한다.
- 사용자 서비스의 기존 입력·저장·미리보기·PDF/X-4 경로는 이번 테스트 연결에서 교체하지 않는다.
- 다른 템플릿과 Published Package 정본은 이번 migration 대상이 아니다.
