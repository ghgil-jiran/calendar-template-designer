# Work Context

## 템플릿 설정 UI 최신 상태 · 2026-09-05

- 새 템플릿 설정은 기본 → 학교 정보 및 에셋 → 일정 등록 → 색상·폰트 테마 → Master → 출력 → 그래픽 → 디자인 유형 설정 → AI 디자인 생성의 9단계다.
- 학교 정보는 텍스트 정보와 이미지 자산을 분리하며 기본 샘플 이미지와 추가 이미지를 역할별 팝업에서 등록·교체·삭제한다.
- 교훈은 텍스트, 교가는 이미지다. 학교 전경 역할의 화면 명칭은 `학교전경/표지`다.
- OpenAI 키는 연결 상태만 기본 표시하고 `키 변경`을 눌렀을 때만 교체 입력란을 연다. 기존 Vault 키는 자동 사용한다.

## 최신 AI 표지 작업 · 2026-09-05

- 현재 독립 프롬프트 규칙은 `school-calendar-prompt@0.2.1`이다.
- 표지 시안 2~4개, low/medium 생성 품질, 네 가지 개별 배치안을 지원한다.
- 선택 결과는 별도 초안에 저장한다. AI 이미지는 배경 자산이며 학교 사진·연도·학교명·슬로건은 편집 개체로 유지하고 선택 배치안을 적용한다.
- Supabase Vault가 연결됨이면 저장된 키를 자동 사용한다. 키 입력란은 교체할 때만 사용한다.

## AI 월별 디자인 세트 기준 · 2026-09-05

- 표지·연력·월력 앞면·월력 뒷면·뒷표지는 공통 조형 언어·서체 위계·선·형태·기본 팔레트를 공유한다.
- 12개월 월력은 월 표시 강조색, 배경 톤, 계절 모티프를 월별로 변주하며 같은 배경을 12회 반복하지 않는다.
- 12개월 월력 뒷면에는 각 월에 대응하는 독립 일러스트 자산 1개를 둔다.
- 달력 격자·월·요일·날짜·일정·연력·미니월력·띠 월력·플래너·체크리스트·메모는 이미지에 굽지 않고 편집 가능한 구조 개체로 유지한다.
- 역할별 AI 결과가 준비된 페이지만 중립화하며 준비되지 않은 역할의 기존 디자인과 공유 Master는 유지한다.
- 월력 뒷면 구성은 참고 템플릿에 고정하지 않고 AI 디자인 생성 조건에서 직접 선택한다.
- 선택 가능한 뒷면 개체는 이미지·뒷면 월력·전달/다음 달 미니 월력·월 목표·할 일 리스트·주별 계획·메모·월력 띠력이며, 선택 결과는 생성 요청과 페이지 배치 계획에 함께 저장한다.

## 현재 상태 · 2026-09-05

- 새 템플릿의 AI 디자인 기준은 참고 템플릿의 디자인 장식을 상속하지 않는 `ai-design-neutral-base.v1`이다.
- 중립 기준은 학교 사진·달력·학교 정보 등 의미 있는 편집 콘텐츠를 보존하고 꽃·블록·배경 프리셋 등의 장식 요소만 제거한다.
- 연력 개체와 월력 뒷면 플래너 표면은 중립 기준에서 투명 배경을 사용해 새 AI 디자인 위에 독립적으로 배치된다.
- AI 생성 결과는 페이지 역할별로 분리되며 표지 생성 자산은 `cover-front`에만 적용된다.
- 중립화도 생성 결과가 준비된 역할에만 적용한다. 표지 결과만 선택한 상태에서는 연력·월력·월력 뒷면의 기존 디자인을 유지한다.
- 공유 Master 장식 원본은 삭제하지 않고 해당 역할 페이지에서만 숨긴다. 월력 뒷면의 구조 개체와 가독성 배경은 항상 보존한다.
- 첫 품질 실험 범위는 표지다. `school-calendar-prompt@0.2.0`은 같은 스타일로 구성 방향이 다른 표지 시안 2~4개를 만들고 배경·장식·빈 사진 프레임만 생성한다.
- 학교 사진·교표·연도·학교명·주소·연락처는 이미지에 넣지 않고 기존 편집 개체로 유지하며, 선택 결과는 `cover-front` 별도 초안에만 적용한다.
- AI 디자인 화면의 공통 문자열 처리 함수를 복구해 참고 페이지·스타일 선택·시안 카드가 중간 오류 없이 렌더링된다.
- AI 디자인 ⑨단계에는 Vault 연결 영역과 분리된 `실제 AI 디자인 생성` 영역이 있으며, 스타일 4종을 선택해 단일 배경 자산 생성을 실행한다.
- Supabase Vault 저장 버튼은 설정 창 공통 클릭 처리와 Enter 입력을 지원하며, 연결 요청이 15초를 넘기면 설정 확인 메시지를 표시한다.
- ⑨단계에 `새 AI 배경 1개 생성`을 추가해 선택한 스타일·학교급·장식·사진·계절·색상 조건을 서버에 전송한다.
- `/api/ai-design-generate`는 Master Admin을 확인한 뒤 OpenAI Images API를 호출하고 WebP 자산 1개와 생성 모델·시각·프롬프트 버전·품질 검사 상태를 반환한다.
- AI 디자인 ⑨단계에서 Master Admin이 OpenAI API 키를 입력하면 서버가 Supabase Vault에 암호화 저장한다. 화면·브라우저 저장소·일반 테이블에는 키를 남기지 않고 연결 여부만 표시한다.
- 동적 생성 API는 Supabase Vault의 키만 서버 내부에서 읽는다. Vault에 값이 없으면 고정 샘플은 계속 사용할 수 있지만 새 동적 생성은 설정 필요 메시지로 중단한다.
- 동적 생성 결과도 기존 스타일 시안 하나를 교체할 뿐이며, 선택 전에는 프로젝트에 적용하지 않는다.
- 실제 AI 생성 샘플 자산 4종(균형형·사계절형·사진 프레임형·학생 친화형)을 ⑨단계 시안 카드에 연결했다.
- 시안은 AI PNG와 기존 편집 개체의 모형을 겹쳐 보여주며, 선택 후 새 초안의 지원 페이지에 `ai-design-background` 이미지 개체로 실제 적용한다.
- 생성 자산은 RGB 1536×1024 원본을 보존하고 버전 manifest와 금지 텍스트·여백·안전 영역 1차 검사 결과를 함께 저장한다.
- 임의 학교 사진이 포함된 생성 결과는 학교 사진 보호 규칙 위반으로 배제했다.
- `새 템플릿 만들기 → 상품·페이지 구성 → 템플릿 설정 ①~⑨` 흐름을 유지하며 AI 디자인 생성은 ⑨단계에만 진입한다.
- ⑨단계에서 선택한 시작 템플릿의 실제 표지·연력·월력·월력 뒷면·뒷표지를 렌더링해 읽기 전용 참고 디자인으로 표시한다.
- 사용자가 고른 참고 항목과 역할별 페이지 ID·템플릿 버전은 `ai-design-generation-request.v1`에 기록된다. 기존 템플릿과 Published Package는 변경하지 않는다.
- AI 디자인 에디터 v1.1의 manifest, 프롬프트 세트, 스타일 규칙, 페이지 규칙, 인쇄 품질 기준을 본체에서 분리된 버전 파일로 저장했다.
- 실제 AI 단일 배경 호출은 연결됐으며 다음 테스트 시작점은 Vault migration 적용과 회사 OpenAI API 키 등록 후 배포 화면 생성 검증이다.

- 작업 대상은 `ghgil-jiran/calendar-template-designer`의 `main`이다.
- 월력 공통 계약 `monthly-calendar-preset.v1`과 기존 `sample-3/sample-6` 호환 계층이 Runtime·Contracts에 저장됐다.
- 샘플 6은 `academic-boxed@1.0.0`, 샘플 3은 `segmented-underline@1.0.0`으로 분리되며 둘 다 5행·6행을 지원한다.
- 샘플 6 원본 PDF의 벡터 치수를 측정해 Studio의 독립 프리셋 카탈로그와 CSS 변수 연결을 로컬 구현했다.
- 새 계약 문서와 기존 샘플 6 문서는 같은 카탈로그를 사용하며, 샘플 3은 박스형 카탈로그로 해석하지 않는다.
- 샘플 3 문서 버전 4는 표지 교훈·슬로건 분리, 연력 제목·요일·주말색, 학교 상징 체목, 독립 미니 월력 스타일, 뒷표지 정보 위계를 포함한다.
- 월력 공통 구성 계약 `monthly-calendar-composition.v1`은 상단 학교 정보·월 제목·요일·날짜 격자의 네 구역과 Binding·경계·충돌 방지 원칙을 정의한다.
- 요일 스타일은 표시 높이와 날짜 격자까지의 간격을 소유하므로 격자 스타일 변경이 요일 기준선을 움직이지 않는다.
- 5×7·6×7 선택은 월 제목·요일 위치를 건드리지 않고 남은 날짜 격자 높이만 5행·6행으로 다시 나눈다.
- 독립 테두리 캡슐의 고정 최소 높이와 바깥 여백이 첫 날짜 행을 침범하던 문제를 제거했고 기간 일정도 같은 시작선을 사용한다.
- 전체 build와 Studio 검사 191개, 인라인 스크립트 23개가 통과했다.
- 편집 페이지는 탁상형 850px·세로형 720px의 고정 설계판으로 렌더링하고 브라우저 폭에는 바깥 표시 배율만 맞춘다.
- 브라우저 크기에 따라 텍스트 상자 폭과 줄바꿈이 달라지지 않으며, 55%보다 작은 축소 대신 캔버스 스크롤을 사용한다.
- 템플릿 라이브러리 썸네일은 편집 화면 배율을 이어받지 않고 고정 설계면을 별도로 복제한 뒤 카드 크기에 한 번만 축소한다.
- 템플릿 생명주기 상태는 `draft·ready·published·archived`이며 `isStandard`는 상태와 독립된 별도 속성이다.
- 표준이 아닌 템플릿은 상태와 관계없이 디자인 편집이 가능하고, 표준 템플릿만 설정과 새로 만들기를 허용한다.
- `이 템플릿으로 새로 만들기`는 템플릿 설정을 먼저 열고 원본과 분리된 새 초안을 생성한다. 새 초안의 변경은 게시된 원본에 영향을 주지 않는다.
- 카드의 `설정`은 이름·설명·Edition·상태·표준 여부를 원본 레코드에 반영하며 디자인 편집과 분리된다.
- 사용자 서비스 템플릿 선택 목록은 게시됨 상태만 읽으며 표준 여부만으로는 배포되지 않는다.
- 영구 저장 스키마에는 `template_projects.is_standard` migration이 추가됐고, migration 전 구 스키마도 버전 메타데이터 fallback으로 읽을 수 있다.
- 템플릿 카드 표지는 대표 사진을 먼저 표시하고 실제 표지를 순차 렌더링하며, 같은 세션 재진입에서는 렌더 결과 캐시를 재사용한다.
- 카드 본문은 세로 높이를 공유하므로 `이 템플릿으로 새로 만들기` 버튼 시작선이 카드마다 같다.
- 버전 이력은 미리보기 없이 이력 확인과 복원만 제공한다.
- 기존 Package 검사는 제거됐고 `인쇄·출력 품질 검사 · 준비 중` 버튼만 추후 작업 자리로 남아 있다.
- 새 템플릿 저장을 취소하면 편집 화면이 아니라 템플릿 라이브러리로 돌아간다.
- 템플릿 설정에서 상태를 저장하면 원격 응답의 `state`와 카드 `status`를 함께 갱신해 카드 배지와 필터에 즉시 반영한다.
- 라이브러리 저장 중에는 진행 애니메이션과 안내 문구를 표시하고 저장·취소 버튼을 잠근다.
- 편집 화면 상단 명칭은 `우리학교인쇄 CALENDAR EDITOR`이며, 왼쪽 개체 분류 탭을 선택하면 샘플 학교 자료와 삽입 적용 범위가 자동으로 접힌다.
- 왼쪽 패널의 상단 자료 영역과 개체 분류는 구분선으로 나뉘며, 접힌 자료 영역은 `자료·적용 범위 펼치기`로 다시 열 수 있다.
- 왼쪽 자료·개체 추가 영역은 제목 11px, 항목명·버튼 10px, 설명 9px의 일관된 글자 위계를 사용한다.
- AI 디자인 에디터 v1.1은 달력 템플릿 에디터의 하위 기능으로 두고, 프롬프트·스타일 규칙·품질 평가 기준을 본체와 별도로 버전 관리한다.
- 사용자 서비스의 초기 디자인 선택은 스타일 3~4종과 구성 타입 4~5종을 독립 조합해 약 12~20개 유형을 제공하는 방향으로 확정했다.
- AI 디자인 결과는 한 장의 배경이 아니라 이미지 자산·편집 개체·배치 설계·스타일 규칙으로 분리하며, 인쇄 조건은 초기부터 적용하고 최종 CMYK·PDF/X-4 변환은 출력 단계에서 수행한다.
- Inspector의 요일·날짜 격자 선택이 프리셋 기본값에 가려지던 회귀는 로컬에서 수정됐고, 기존 저장값과 새 `calendarOverrides`를 모두 지원한다.
- 열린 가로줄의 요일 바로 아래 경계 누락도 첫 날짜 행 전용 상단선으로 로컬 수정됐다.
- 월 제목과 요일 사이에는 원본 측정값 1.7mm의 프리셋 내부 여백을 적용하며 날짜 격자 높이는 유지한다.
- 날짜 격자는 `전체 박스`, 첨부 디자인 1 기반 `독립 밑줄`, 첨부 디자인 2 기반 `미니멀 무선`, `개별 사각 셀` 네 가지 표현을 Inspector에서 선택할 수 있다.
- 네 표현은 같은 날짜 계산·일정 데이터·5행·6행 구조를 사용하며, 첨부 디자인 2의 삽화·페이지 편집 구성은 날짜 격자 옵션의 범위에 포함하지 않는다.
- 월 표시 6종은 표현별 최소 제목 높이(10~18%)를 사용해 큰 월 숫자가 연도·영문월 또는 요일을 침범하지 않으며, 기존 한글형 저장값도 호환한다.
- 재검토 결과 큰 숫자형 최소 제목 높이는 18~20%로 상향했으며 월 표시 위치는 왼쪽·가운데·오른쪽을 지원한다.
- 개별 사각 셀의 기간 일정은 하나의 일정 데이터와 라벨을 유지하되 셀 사이 6px 간격에서는 색상 막대가 끊어져 카드 구조를 침범하지 않는다.
- 편집 화면은 중복된 `편집·삽입·정렬·보기` 메뉴 행을 제거했으며, 정렬은 도구상자, 현재·전체 미리보기는 상단 `미리 보기` 드롭다운에서 제공한다.
- 하단 페이지 구성은 112px의 간결한 가로 탐색 스트립이며 면 번호·역할·현재/전체 위치만 표시한다.
- 샘플 3 인쇄 PDF(260×180mm, 28면)를 기준으로 별도 `[학사달력] 탁상형 검토 02 - 이미지 미니월력` 초안을 로컬 구현했다.
- 샘플 3 초안은 6번 및 Published Package와 분리되며, 독립 요일 캡슐·세로선 없는 날짜 격자·3분할 월별 이미지·현재/전후월 미니월력과 표지·연력·학교상징·뒷표지 전용 면을 가진다.
- 샘플 3 초안은 전체 build, Studio 검사 184개, 인라인 스크립트 21개를 통과했으며 GitHub `main@b3525bf`와 Vercel Production에 저장·배포됐다.
- 선택 화면 위에 편집 도련선·안전선이 남는 문제는 가이드 가상 요소의 높은 z-index와 프로젝트 전환 시 잔류 클래스가 함께 만든 결함으로 확인했으며 로컬에서 두 경로를 모두 차단했다.
- 공통 페이지 네비게이션은 하단 전체 폭 스트립에서 왼쪽 개체 추가 영역 하단의 2열 세로 패널로 이동했으며, 홀수 면 강조와 표지·월력·간지·뒷표지 역할별 색상 구분을 적용했다.

## 다음 할 일

- 현재 구현 상태: `ai-design-quality.v1@0.1.0`이 전체 적용 결과의 가독성·충돌·안전 영역·금지 텍스트·중복·일관성·해상도를 28면별로 검사한다. 이미지 생성으로 해결 가능한 실패만 해당 역할·해당 월 단위로 한 번 재생성하고, 배치 충돌·안전 영역·최종 300dpi 확인은 편집·출력 검토 항목으로 남긴다.
- 작업 잠금: 사용자가 순서를 변경하기 전까지 아래 1~10단계 외의 새 작업 흐름을 시작하지 않는다.

1. ⑧ 디자인 유형 UI 완성: 6개 역할 모두에 와이어프레임, 핵심 개체, 추천 용도, 편집 주의를 표시했다. **완료**
2. 디자인 스타일과 6개 페이지 유형을 `school-calendar-design-types@0.1.0` 카탈로그로 분리하고 화면이 이를 읽게 했다. **완료**
3. 선택 조합을 `ai-design-spec.v1@0.1.0` 디자인 설계 JSON으로 만들고 저장·재열기 시 복원한다. **완료**
4. 페이지 유형을 표지·연력·학교 상징·월력·월력 뒷면·뒷표지의 실제 편집 개체 배치에 적용한다. **완료**
5. 월 표시·요일·날짜 격자·사진 프레임·미니 월력·플래너 등 개체 스타일 프리셋을 연결한다. **완료**
6. 설계 JSON을 `school-calendar-design@0.5.0`의 역할별 프롬프트에 연결한다. **완료**
7. 전체 방향을 판단할 대표 디자인 세트 2안을 생성하고 6개 역할 전체를 비교·선택한다. **완료**
8. 선택 세트를 나머지 11개월과 전체 페이지에 확장한다. **완료**
9. 가독성·충돌·안전 영역·금지 텍스트·중복·일관성·해상도를 자동 검사하고 실패 역할만 재생성한다. **완료**
10. 저장·재열기·템플릿 라이브러리·사용자 서비스·28면 전체·PDF 출력까지 통합 검증한다. **현재 단계**

- 정확한 다음 시작점은 10단계 저장·재열기·템플릿 라이브러리·사용자 서비스·28면 전체·PDF 출력 통합 검증이다. 디자인 유형은 디자이너의 편집 가능한 시작 구성이지 완성 템플릿이 아니다.

## AI 디자인 에디터 현재 기준 · 2026-09-04

- 상위 제품은 `달력 템플릿 에디터 v1.3`, 독립 하위 모듈은 `AI 디자인 에디터 v1.1`이다.
- 생성 기능은 새 템플릿의 단계별 `템플릿 설정`에서 시작하고, 선택 결과를 별도 초안으로 만든 뒤 편집 화면에서 세부 조정한다.
- 독립 버전 대상은 모듈, 프롬프트 세트, 디자인 스타일 규칙, 페이지 역할별 생성 조건, 디자인 품질 평가 기준과 참고 분석 결과다.
- 기존 Published Package, 검토 01·02, 사용자 서비스 입력·저장·미리보기·PDF 경로와 기존 대시보드를 덮어쓰거나 임의로 교체하지 않는다.

## 이전 안정 상태 · 2026-09-02

- 작업 대상은 `ghgil-jiran/calendar-template-designer`의 `main`이며 탁상형 표준 01 문서 버전은 v13이다.
- 연력은 기본적으로 월력 행 수를 따르고, 개체 스타일에서 월력 설정 상속·월별 자동 5/6주·항상 5주·항상 6주를 선택할 수 있다.
- 템플릿 기본 설정의 5×7·6×7 선택은 페이지 구조에 적용되며 저장 후 고정 행 모드로 유지된다.
- 6번 템플릿에서는 3번 빠른 디자인 조합을 선택할 수 없다. 이전 작업본에 3번 조합이 저장돼 있으면 v13 migration이 6번 원본형 박스 격자와 넓은 세로 영역으로 복구한다.
- 빈 날짜 셀 미니 월력은 마지막 주에 연속 빈 칸 2개가 있을 때만 표시하고, 대상 월의 실제 5/6주 구조를 사용한다.
- 월력의 상단·왼쪽 선을 보강했으며 템플릿 설정 창이 열리면 도련·안전선은 잠시 숨긴다.
- 전체 build와 Studio 검사 166개, 에디터 인라인 스크립트 21개가 통과했다.
- 첨부 기준 자료는 탁상형 6번 월목표·할 일 플래너 PDF와 3번 이미지·월력·미니달력 PDF다.
- 템플릿 카드 이름은 `[학사달력] 달력유형 표준|검토 NN - 특징` 규칙을 사용하며 내부 Package 버전은 별도로 표시한다.
- 템플릿 에디터의 활성 카드는 `[학사달력] 탁상형 검토 01 - 월별 플래너`, `[학사달력] 탁상형 표준 02 - 월별 이미지`, `[학사달력] 벽걸이형 검토 01 - 이미지 월력`으로 정리됐다.

## 이전 확인 항목

1. Production에서 기존 표준 01 작업본을 다시 열어 v13 migration과 6번 원본형 복구를 확인한다.
2. 템플릿 설정에서 5×7과 6×7을 각각 저장해 월력 전체와 연력 상속 결과를 확인한다.
3. 연력 스타일의 네 가지 행 수 선택과 설정 창의 도련선 숨김을 확인한다.
4. 템플릿 에디터와 사용자 서비스의 카탈로그·자료수집 카드에서 상태·이름·내부 버전·면 구성이 같은지 나란히 확인한다.
5. 사용자 서비스 `integration/runtime-v2`에서 갱신된 템플릿을 선택해 28면 미리보기·저장·재열기 결과를 확인한다.

## 보존 원칙

- 기존 저장 원본은 불러오는 순간 덮어쓰지 않고 사용자가 저장할 때만 v13으로 기록한다.
- 사용자 서비스의 기존 입력·저장·미리보기·PDF/X-4 경로는 이번 테스트 연결에서 교체하지 않는다.
- 다른 템플릿과 Published Package 정본은 이번 migration 대상이 아니다.
