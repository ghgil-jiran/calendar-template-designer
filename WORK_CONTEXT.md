# Work Context

## 최소 페이지 구조 정책 · 2026-09-11

- 탁상달력 12개월 최소 구조는 표지 1면 + 월력 앞뒤 24면 + 뒷표지 1면의 26면으로 확정했다.
- 26면형은 첫 월력 뒷면을 표지 뒷면으로 함께 사용하며, 별도 표지 뒷면을 쓰면 양면 짝을 맞춰 최소 28면이 된다.
- 달력 유형의 페이지 구조 정책에서 콘텐츠 항목인 `연간 한 면`을 숨기고 `표지 뒷면 사용`, `뒷표지 뒷면` 정책과 실시간 최소 면수 계산을 추가했다.
- 탁상달력의 필수 면 정책이 어긋나거나 공유형과 월력 뒷면 정책이 충돌하면 유형 저장을 막는다.
- 새 공유형 템플릿은 실제 26면으로 재구성하고, 이후 간지 수 변경 때도 공유 정책을 유지한다. 기존 저장 템플릿과 Published Package는 자동 변경하지 않는다.
- 페이지 설정의 구성 순서는 표지 → 표지 뒷면 → 간지 → 월력 앞면 → 월력 뒷면 → 뒷표지 → 뒷표지 뒷면으로 정리했다.
- Supabase 적용용 migration은 `202609110001_calendar_type_surface_policy.sql`이며 사용자가 직접 적용해야 한다.
- 전체 빌드와 Studio 회귀검사 390개가 통과했다. 클라우드 브라우저는 로컬 주소 접근이 차단되어 자동 육안 검사는 수행하지 못했다.

## 현재 이어서 할 작업 · 2026-09-10

- 작업 시작 기준은 `ghgil-jiran/calendar-template-designer`의 `main@9405814`이며, 1차 공간 계약 변경은 GitHub `main@3a87597`에 저장했다.
- 현행 공식 디자인 스타일은 에디토리얼 그래픽, 캠퍼스 다큐멘터리, 모듈러 컬러 시스템, 컨템퍼러리 일러스트레이션, 디지털 오라 모션, 한국적 모던 그래픽의 6종이다.
- 과거 12개 스타일은 폐기하되 기존 저장 템플릿과 Published Package 참조를 먼저 조사하고 자동 변경하지 않는다.
- AI 생성 조건에 실제 편집 개체의 유형, 중요도, 가독성, 점유율, 형태, 보호 강도, 안전 여백, 겹침 허용 방식을 연결했다.
- 월력 뒷면은 사진·달력·플래너·메모 개체가 실제 생성되기 전에도 선택한 구성의 예정 좌표와 크기를 AI 보호 조건에 포함한다.
- AI 프롬프트는 중요한 달력·학교 정보 개체를 페이지 구성의 중심축으로 취급하고, 사진 영역에는 분위기만 겹치며, 보호 좌표를 흰 카드나 프레임으로 표현하지 못하게 한다.
- 과거 12개 스타일은 활성 프리셋과 프롬프트 선택지에서 제거했고 기존 저장값은 현행 6종으로 읽기 마이그레이션한다. Published Package는 변경하지 않는다.
- 사진 콜라주는 월력 뒷면에서 독립적으로 교체 가능한 이미지 프레임 3개로 확장되고 세 프레임 모두 AI 보호영역에 포함된다. 첫 프레임만 기존 월별 이미지 데이터에 연결하며 보조 프레임은 잘못된 다른 월 사진을 참조하지 않는다.
- 표지 사진 콜라주와 월력 이미지형은 실제 교체 가능한 이미지 프레임을 만들며, 연력 분할 정보와 학사일정 간지는 `calendar.events`를 읽는 실제 `event-list` 개체를 만든다.
- 모든 현행 편집 개체 유형은 텍스트·이미지·달력·일정·플래너·그래픽·학교 의미 개체·향후 개체의 명시적 스타일 그룹을 받는다.
- 전체 `npm run build`가 통과했다. Studio 회귀검사 372개, 전체 패키지 검사, Sprint 2 제품 검사와 스타일 검사가 모두 통과했다.
- Supabase 변경은 없다.

## Production 실물 검토와 후속 보정

1. Production에서 캠퍼스 다큐멘터리 대표 6면과 월력 12개월·24자산 생성을 완료했고 28면 전체 적용을 확인했다.
2. 달력 앞면은 가독성과 보호영역은 안정적이지만 같은 사선 띠와 메모지 형태가 반복되어 스타일·월별 차이가 약했다.
3. 달력 뒷면은 세 사진 프레임·미니 달력·메모가 겹치지 않고 잘 배치됐지만, AI 배경에 연필·메모지·핀·잎 등 문구류 장식이 생성되어 캠퍼스 다큐멘터리 규칙과 어긋났다.
4. 보조 사진 프레임이 2월·3월 데이터 바인딩으로 오인되던 연결을 제거하고, 샘플 사진은 월별 순환 대신 3개월 단위 계절 묶음으로 맞춘다.
5. 캠퍼스 다큐멘터리 월력 프롬프트에는 물리적·일러스트 문구류와 사진 코너·테이프·크롭 표식까지 명시적으로 금지하고 비재현 색면·짧은 선·추상 표식만 허용한다.
6. 브라우저의 검토용 PDF는 파일 다운로드가 아니라 `window.print()` 기반이어서 자동 저장되지 않았다. 다음 Production 검증에서 사용자가 인쇄 대화상자의 PDF 저장을 직접 확인해야 한다.
7. 사용자가 첨부한 `20260910_캠퍼스 다큐멘터리_01.pdf`는 260×180mm 상당의 30면이며, 표지·연력·학교상징·학사일정·12개월 앞뒷면·뒷표지 2면을 포함한다.
8. PDF 기준으로 월력과 사진 콜라주의 개체 간 겹침은 안정적이지만 표지 학교 정보가 하단에서 잘리고, 빈 표지 프레임 안내가 PDF에 남으며, 학사일정이 하단 작은 영역에 몰린다.
9. 캠퍼스 다큐멘터리 월력 배경에는 수채 식물·종이·핀·붓질이 여전히 나타나고 월력 앞면은 같은 상단 선과 점 구성이 반복된다. 프롬프트를 평면 벡터 색면·짧은 선·기하 점으로 더 좁히고 재질·소품·동일 구도 반복을 명시적으로 금지한다.
10. 표지 정보와 로고를 안전영역 안에서 역할별 크기로 재배치하고, 학사일정 목록을 본문 84×70% 영역으로 확대하며, 검토 PDF에서는 비어 있는 이미지 프레임 전체를 제거한다.

## 페이지 구성·실제 배치 2차 · 2026-09-10

- 표지 공식 구성은 `중앙 사진형`, `왼쪽 사진형`, `오른쪽 사진형`, `자유 구성형(대표 사진 없음 포함)` 4종이다.
- 각 표지 구성은 실제 260×180 비율에서 3개 대표 배치를 가지며, 페이지 설정 미리보기와 편집기 초기 배치가 같은 좌표를 사용한다.
- 표지의 학교 사진은 샘플 이미지가 연결된 교체 가능 개체다. AI는 학교 전경을 새로 만들지 않고 사진·연도·교표·학교 정보의 실제 영역을 보호한다.
- 표지 배치 결과는 개체 유형, 좌표, 점유율, 중요도, 가독성, 보호 강도와 안전 여백을 AI 생성 요청에 전달한다.
- 학교 전경·교표 같은 이미지 개체의 내부 역할명은 출력 제목이 아니다. 교훈·교가·교목·교화만 기본 제목을 표시하며 개체별로 숨길 수 있다.
- 간지 공식 구성은 `학교 상징`, `학교 소개`, `Yearly Plan`, `연력`, `사용자 정의` 5종이다. 제목은 강제하지 않고 포함 개체에서 선택한다.
- Yearly Plan은 전체 페이지형을 기본으로 배치한다. 간지별 실제 대표 배치는 사용자가 제공할 사례 3종을 기준으로 후속 확정한다.
- 월력의 교표 충돌과 월력 뒷면 선택 개체 최적 배치는 구조 기반의 우선 보정을 적용했으며, 실제 사례를 받은 뒤 대표 배치를 확정한다.
- 전체 `npm run build`가 통과했다. Studio 회귀검사 377개와 전체 패키지·제품·스타일 검사가 통과했다.
- 다음 사용자 확인 지점은 Preview에서 표지 4종의 구성 선택, 3개 미리보기, 편집 진입 후 실제 개체 위치와 대표 AI 생성 결과다.

## 학교 상징 대표 배치 · 2026-09-10

- 실물 사례를 기준으로 학교 상징 공식 배치를 `개별 카드형`, `좌우 분할형`, `오픈형`, `구분선형` 4종으로 정의했다.
- 페이지 설정은 `구성 형태 → 포함 개체 → 대표 배치` 순서로 보여주며, 학교 상징을 선택했을 때만 네 가지 실제 구도 미리보기를 표시한다.
- 교가는 악보 가독성을 위해 오른쪽 40% 폭·74% 높이의 큰 영역을 기본으로 사용하고 자르기를 금지한다. 교가를 선택하지 않으면 교훈·교목·교화가 전체 폭으로 자동 재배치된다.
- 카드와 구분선은 AI 이미지에 포함하지 않고 편집 가능한 개체 표현 속성으로 적용한다.
- AI 보호 정보에는 교가의 실제 좌표, 높은 중요도, 필수 가독성, 추가 여백과 각 개체의 박스·구분선 표현을 포함한다.
- 전체 `npm run build`가 통과했다. Studio 회귀검사 380개와 전체 패키지·제품·스타일 검사가 통과했다.
- 다음 사용자 확인 지점은 Preview의 학교 상징 4종 미리보기, 개체 선택 변경 시 재배치, 편집 화면의 카드·구분선 표현이다.

## 표지·뒷표지 물리 면 명칭 및 자동 구조 · 2026-09-11

- 화면 명칭을 `표지`, `표지 안쪽면`, `뒷표지 안쪽면`, `뒷표지`로 통일하고 기존 내부 role ID는 호환을 위해 유지한다.
- 달력 유형에는 `표지 안쪽면을 첫 월력 뒷면으로 함께 사용` 체크 하나만 제공한다. 체크하면 26면이며 별도 표지·뒷표지 안쪽면이 없고, 해제하면 두 안쪽면이 함께 생성되는 28면이다.
- 연력은 물리 구조 정책에서 제거하고 표지 안쪽면·간지·뒷표지 안쪽면의 콘텐츠 구성으로만 취급한다.
- 26면 압축 시 `back-cover-front`가 아니라 실제 마지막 외부 면인 `back-cover-back`을 뒷표지로 유지한다.
- 페이지 설정은 실제 페이지가 존재할 때만 표지 안쪽면과 뒷표지 안쪽면 카드를 표시한다.
- 전체 빌드와 Studio 회귀검사 390개가 통과했다.
- Preview 실사용 확인에서 발견된 기본 설정의 파생 정책 `undefined`, 유형 저장 피드백 부재, 26면에서 안쪽면 설정 카드가 남는 문제를 수정했다.
- 기본 설정은 연력·뒷표지 파생 정책을 숨기고 표지 안쪽면 공유 상태를 명시한다. 저장 버튼은 저장 중·완료·실패를 직접 표시한다.
- 새 템플릿에는 유형 제약을 먼저 적용한 뒤 26면 압축을 실행하며, 공유형에서는 페이지 데이터가 오래된 경우에도 표지·뒷표지 안쪽면 설정 카드를 강제로 숨긴다.
- 수정 후 전체 빌드와 Studio 회귀검사 392개가 통과했다.

## 보존 원칙

## 뒷표지 대표 배치 · 2026-09-11

- 실물 사례를 기준으로 뒷표지 공식 유형은 `교표 + 학교 정보형`, `연도 + 교표 + 학교 정보형`, `연도 + 학교 전경 + 교표 + 학교 정보형` 3종이다.
- 각 유형은 3개 대표 배치를 제공하며 선택한 유형에 맞춰 기본 포함 개체가 함께 바뀐다.
- 학교 전경은 AI가 만드는 배경이 아니라 사용자가 교체할 수 있는 실제 사진 개체다. 개체 이름·제목·캡션은 출력하지 않는다.
- 실제 개체 배치 좌표가 AI 보호 영역으로 전달되므로 AI는 학교 사진을 새로 만들거나 정보 영역에 초점 소재를 배치하지 않는다.
- 서버 허용 목록과 프롬프트도 새 유형 ID로 동기화했다.
- 전체 빌드와 Studio 회귀검사 386개가 통과했다. 다음 확인은 Preview의 뒷표지 앞면·뒷표지 카드에서 유형 전환, 3개 미리보기, 편집 진입 후 실제 배치다.

## 월력 뒷면 대표 배치 · 2026-09-10

- 실물 사례를 기준으로 `대형 이미지·미니월력형`, `월간 계획표 중심형`, `이미지·메모 복합형`, `사진 콜라주·띠력형` 4종을 공식 대표 배치로 정의했다.
- 월간 계획표 중심형은 왼쪽 보조 정보열에 작은 교체 사진, 현재 월력, 주요 학사일정을 세로 배치하고 오른쪽 60% 폭을 1~31일 날짜별 계획표로 사용한다.
- 주요 학사일정은 `calendar.events`에 연결되는 실제 일정 목록 개체이며, 샘플 일정과 사용자 업로드 일정이 같은 바인딩으로 표시된다.
- 날짜별 계획표는 31행 편집 개체로 생성한다. 사진 콜라주는 1~6개 이미지 프레임을 지원하고 대표 3개 프레임을 조화롭게 겹치며 하단 띠력은 별도 달력 개체로 유지한다.
- 선택한 개체의 실제 좌표·크기와 함께 중요도·가독성 요구·보호 영역을 AI 생성 요청에 전달해 배경 이미지가 일정·계획·달력 영역을 침범하지 않게 한다.
- 과거 `image-led`, `balanced-grid`, `planner-led` 저장값은 새 4종의 대응 배치로 읽기 마이그레이션하고 Published Package는 변경하지 않는다.
- 전체 `npm run build`가 통과했다. Studio 회귀검사 381개와 전체 패키지·제품·스타일 검사가 통과했다.

## 월력 앞면 상단 대표 배치 · 2026-09-10

- 기존 월력 격자 표현과 미니월력 설정은 변경하지 않고, 달력 외부의 상단 정보 구성만 `좌우 정보 분산형`, `중앙 월제목·좌측 학교정보형`, `중앙 강조 블록형`, `좌측 월제목형` 4종으로 정의했다.
- 월력 선택 개체는 교표, 학교명, 교훈, 슬로건, 교체 가능한 사진 프레임으로 제한한다. 학교 주소, 연락처, 웹사이트는 월력 선택 항목과 저장 규격에서 제외한다.
- 과거 저장본에 월력 주소·연락처·웹사이트가 남아 있어도 새 시작 구성을 적용할 때 해당 AI 보조 개체를 제거한다.
- 대표 배치 미리보기, 실제 월 제목·달력 영역, 선택한 학교 개체 좌표와 AI 보호 정보가 같은 배치 좌표를 사용한다.
- 달력 격자는 상단 구성 아래 `x 5%, y 15%, width 90%, height 80%`를 기본 영역으로 확보해 이전 생성 결과보다 위쪽에서 시작한다.
- 전체 `npm run build`가 통과했다. Studio 회귀검사 383개와 전체 패키지·제품·스타일 검사가 통과했다.

## 페이지 구성 단일 원본 통합 · 2026-09-10

- 페이지 설정 상단의 중복 `페이지별 시각 구성` 카드를 제거하고 역할별 `시작 구성`을 표지·표지 뒷면·월력·뒷표지 앞뒷면의 단일 설정 원본으로 확정했다.
- `pageSettings.roleCompositions[role].typeId`가 실제 적용과 AI 프롬프트의 페이지 유형을 함께 결정하며, 별도의 `pageTypes` 값은 정규화 시 이 값에서 자동 동기화한다.
- 기존 템플릿에 역할별 시작 구성이 없을 때만 과거 `pageTypes` 값을 새 시작 구성으로 이관한다. 둘 다 있으면 실제 적용되던 역할별 시작 구성을 우선한다.
- 간지는 물리 면별 `dividerPages[pageId]`, 월력 뒷면은 전용 구성 형태·개체·대표 배치 설정을 각각 단일 원본으로 유지한다.
- 월력 뒷면 구성 형태 선택을 전용 세부 설정에 추가해 중복 카드 제거 뒤에도 모든 구성 유형을 선택할 수 있다.
- 전체 `npm run build`가 통과했다. Studio 회귀검사 384개와 전체 패키지·제품·스타일 검사가 통과했다.

- 기존 Published Package와 저장 템플릿을 자동 변경하거나 교체하지 않는다.
- 학교 정보, 달력, 일정, 교표, 사진 프레임은 AI 이미지에 굽지 않고 편집 개체로 유지한다.
- 사용자 미커밋 변경과 `tmp/`는 폐기하거나 저장 범위에 포함하지 않는다.
- Supabase 변경이 필요하면 SQL migration을 작성하고 사용자가 직접 적용한다.
