# Work Context

## 현재 상태 · 2026-09-01

- 기준 저장소는 `ghgil-jiran/calendar-template-designer`의 `main@9449f13`에서 시작했다.
- 신규 시스템 베이스는 `tpl-2028-desk-planner-standard-01`, 표시 이름은 `탁상형 표준 01 · 월별 플래너형`, 템플릿 키는 `desk-sample-6`, 상태는 `draft`다.
- 6번 실물 PDF 전체 28면을 다시 확인했으며 정확한 순서는 1면 표지, 2면 연력, 3면 학교 상징, 4면 3월 플래너, 5면 3월 월력부터 26면 2월 플래너, 27면 2월 월력, 28면 뒷표지다.
- 신규 시스템 베이스도 이 물리적 넘김 순서를 그대로 생성한다. 월별 순서는 `플래너 → 월력`이며 첫 월력은 5면에서 시작한다.
- 기존 저장본을 조용히 덮어쓰지 않고 불러온 작업 사본에만 적용하는 `deskPlannerStandard01` 문서 migration을 추가했다.
- 문서 버전 4 migration은 `desk-sample-6`이면서 월력 앞뒤 각 12면을 가진 28면 또는 이전 30면 작업 사본에만 적용한다.
- 문서 버전 5에서 표지·뒷표지의 중복 학교명·영문명 개체를 제거하고 교표 연결 개체만 유지했으며, 표지·연력·뒷표지 배경을 6번 원본에 더 가깝게 보정했다.
- 빈 날짜 셀 미니 월력은 이제 기본 설정에 따라 5×7과 6×7 월력 모두에서 표시된다.
- 이전 30면 작업 사본은 학교 소개·교육 비전 면을 제거하고 학교 상징을 3면, 뒷표지를 28면으로 이동한다.
- 학교 정보·이미지·월별 파스텔·페이지 데이터는 migration 과정에서 유지한다.
- 흐린 `YYYY년 M월의 뒷면` 안내가 기존 저장본에 남던 원인은 `sampleFamily` 누락으로 확인되어 migration으로 보완한다.
- 관련 단위검사 1건, JavaScript 문법검사, `git diff --check`가 통과했다. 기존 159개 Studio 검사와 전체 build·배포 검증은 반복하지 않았다.
- 첨부 기준 자료는 6번 월목표·할 일·주간 플래너 PDF와 3번 이미지·월력·미니달력 PDF다.
- 편집 화면은 왼쪽 `샘플 학교 자료·개체 삽입`, 가운데 `페이지 편집`, 오른쪽 `페이지/선택 개체 속성`, 하단 `페이지 구성·선택`으로 역할을 분리했다.
- 개체 삽입은 `학교·달력·도형/프레임·기본` 네 범주로 정리했으며 기존 개체 생성·데이터 연결·Master 적용 동작을 그대로 사용한다.
- 왼쪽 공통 검색과 현재 페이지/Master 삽입 범위 선택을 연결했고, 오른쪽은 모든 개체 유형에서 데이터 연결과 사용자 편집 권한을 확인·저장할 수 있다.
- `현재 페이지`에서 Master 개체를 선택하면 페이지 전용 대체 사본으로 분리하며, 캔버스 리사이즈와 오른쪽 속성 저장 모두 다른 Master 페이지의 원본을 유지한다.

## 다음 할 일

1. 배포 화면에서 같은 Master의 두 월을 열어 `현재 페이지` 수정은 한 면에만, `같은 Master 전체` 수정은 두 면 모두에 적용되는지 육안 확인한다.
2. 6번 PDF의 대표 면과 새 화면을 나란히 비교해 제목 크기·색상·선·면·여백을 최종 보정한다.
3. 표지·학교 상징·뒷표지의 학교 정보와 샘플 이미지 교체 흐름이 적절한지 확인한다.
4. 대표 월력 뒷면 승인 뒤 공통 Master가 12개월에 동일 적용되는지 표본 월만 확인한다.
5. 디자인 기준선 확정 뒤에만 신규 Package 발행, PDF/X-4, 사용자 서비스 28면 E2E를 진행한다.

## 반복하지 않는 검사

- 회귀 증거가 없는 기존 Studio 자동검사 159개
- Runtime·Integration·Contracts와 전체 build
- GitHub·Vercel Production 배포 검증
- 이미 확인된 월별 파스텔 전환 검증

## 보존 원칙

- 기존 저장 원본은 불러오는 순간 덮어쓰지 않고, 사용자가 다음에 저장할 때만 새 문서 버전으로 기록한다.
- 다른 템플릿과 Published·Review Package 정본은 migration하지 않는다.
- 신규 Package 발행과 공개 승격은 별도 승인 전 진행하지 않는다.
- 현재 체크포인트는 다음 시각 비교 작업을 준비한 상태이며 디자인 완료나 인쇄 승인 상태가 아니다.
