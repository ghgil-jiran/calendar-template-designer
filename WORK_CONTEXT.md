# Work Context

## 현재 상태 · 2026-08-31

- 현재 브랜치: `main`
- 시스템 베이스의 활성 표준은 탁상형 `desk-academic-standard@1.4.0`과 벽걸이형 `wall-academic-standard@0.3.0` 중심으로 정리했다.
- 탁상형 `1.4.0`은 게시된 Package 정본이며, 벽걸이형 `0.3.0`은 사용자 기능 검증용 Review 표준이다.
- 초기 달력 유형 검증용 시스템 베이스 7종은 삭제하지 않고 보관 상태로 전환했으며 기본 활성 목록에는 표시하지 않는다.
- 시스템 베이스를 편집하면 원본을 변경하지 않고 Package 출처가 연결된 내 템플릿 작업 사본이 만들어진다.
- 내 템플릿 작업 사본은 명시적으로 저장할 때 Supabase에 새 버전으로 저장되며, 다시 열어도 Package 출처를 유지한다.
- 브라우저 저장 레코드는 동일 ID의 시스템 베이스 정본과 상태를 덮어쓸 수 없다.
- 새 연결 작업본은 이전 작업본의 일정 파일 표시와 미리보기를 남기지 않고 `샘플 일정 파일 없음`으로 시작한다.
- Package 로딩이 끝난 뒤 라이브러리 창을 닫아 썸네일용 탁상형 화면이 먼저 보이지 않도록 했다.
- 최초 편집 화면의 실제 폭이 확정된 뒤 달력을 다시 계산하고, 요일 행 최소 높이와 줄 높이를 고정하여 처음에 행이 접히는 현상을 수정했다.
- 관련 회귀검사 46건과 전체 Studio 검사 158건, 제품 상호작용·인라인 스크립트·Vercel 정적 출력 검사가 통과했다.
- 사용자 서비스에서는 벽걸이형 `0.3.0`을 Registry `review`로 유지하면서 Production 첫 화면에 `표준 테스트`로 노출하는 별도 경로를 사용한다.
- 벽걸이형 PDF/X-4 인쇄 Worker는 아직 탁상형 `266×186mm`에 고정되어 있으므로 벽걸이형 정식 인쇄 보증과 `published` 승격은 후속 작업이다.

## 다음 할 일

1. 배포 화면에서 최초 편집 진입 시 요일 머리글이 가려지지 않는지 확인한다.
2. 사용자 서비스의 템플릿 선택 구조를 유형 선택 → 해당 유형의 템플릿 선택 흐름으로 개선한다.
3. 이후 신규 표준 템플릿을 유형별로 하나씩 제작하고 사용자 서비스·인쇄 결과를 검증한다.

## 보존 원칙

- 게시·Review Package 정본을 직접 덮어쓰지 않는다.
- 디자인 변경은 연결된 작업 사본에서 시작하고 검증 후 새 Package 버전으로 발행한다.
- 사용자 서비스의 내장 `createSampleDoc` 탁상형은 인쇄·디자인 비교 기준으로 유지한다.
- 벽걸이형 `0.3.0`은 테스트 노출과 정식 인쇄 보증을 구분한다.
- 기존 실험 템플릿은 삭제 대신 보관하여 필요할 때만 참고한다.
