---
name: schoolp-run
description: 로컬 개발 서버를 실행해 화면을 본다. "실행해줘", "시작해줘", "켜줘", "로컬에서 보여줘" 요청에 사용.
---

`AI_COMMANDS.md`의 "실행해줘" 절차를 그대로 따른다.
- `npm run dev`, 주소는 `http://localhost:3000` 고정(다른 포트 우회 금지).
- 3000 포트가 사용 중이면 기존 dev server 종료 후 재실행. Windows는 `start-windows.cmd` 우선.
