# AI 작업 지시

이 파일은 Codex용 얇은 안내 파일입니다. 실제 정본은 `AI_START_HERE.md`입니다.

작업을 시작하기 전에 반드시 아래 순서로 확인합니다.

1. `AI_START_HERE.md`
2. `STARTER_VERSION.md`
3. `CHANGELOG.md`
4. `AI_COMMANDS.md`
5. 요청과 관련된 `docs/*` 문서

핵심 원칙:

- 답변은 누구나 바로 이해할 수 있게 씁니다. 결과와 다음 할 일부터 말하고, 어려운 용어(에이전트·워크플로우·캐시·훅·FK·모델명·P0 등)와 AI 내부 동작(백그라운드/캐시/단계명) 설명은 피합니다. 없는 존칭을 만들어 붙이지 않고 담백한 존댓말을 씁니다. 확인이 필요한 항목은 계속 모으되 "결정 필요" 같은 딱딱한 라벨 대신 부드럽게 전합니다. 자세한 기준은 `AI_START_HERE.md`의 "팀원 대상 커뮤니케이션 규칙".
- 안내 페이지 URL만 받으면 그 페이지는 요약본이다. 먼저 `schoolp-nextjs-app.zip`/`starter-manifest.json`을 읽고 `requiredDocs`를 현재 파일과 대조한 뒤 부족분만 반영한다. 페이지 요약만으로 기준을 적용하지 않는다.
- 새 세션의 첫 응답이거나 사용자가 커맨드 목록을 물어보면 자주 쓰는 공식 요청 표(6개)를 보여줍니다. **팀원은 그냥 한국어로 말하면 되고**, 슬래시(`/schoolp-*`)는 보조입니다 — 한국어 문장으로 말하거나 슬래시로 부르면 AI가 해당 스킬을 실행합니다(Codex·Claude Code 공통). 공식 6개: 실행/작업 저장/불러오기/배포/핸드오프(`/schoolp-handoff`)/도움말(`/schoolp-help`). 새 화면·기능은 만들 내용을 설명하면 AI가 기획 브레인스토밍(`/schoolp-brainstorm`)부터 자동으로 시작하므로 표에 명령으로 넣지 않습니다(직접 부르려면 "기획해줘"). 점검·최신 기준 확인·Supabase 열기도 표에 없고 말로 부르거나 자동 처리되며, 이어하기 메모(`WORK_CONTEXT.md`)는 저장/불러오기 때 자동 갱신·브리핑됩니다. 자세한 기준은 `AI_COMMANDS.md`.
- 세션이 시작되면 SessionStart 훅(`node scripts/schoolp-update-check.mjs --hook`)이 원격 manifest와 현재 기준을 자동 비교합니다. 오래됐다는 안내가 주입되면 **첫 응답 맨 앞에서, 원래 요청을 처리하기 전에 예/아니오로 먼저 묻습니다**("스타터 업데이트가 있어요(rXX→rYY). 지금 반영할까요? 1) 네 2) 나중에"). "네"면 기존 화면·기능·스타일을 보존하며 부족한 기준만 안전 반영(스킬·설정·scripts 복사, 세션 재시작 안내) 후 원래 요청 처리 + 한 줄 보고, "나중에"면 그냥 진행하고 이번 세션엔 다시 묻지 않습니다.
- 로컬 실행은 `http://localhost:3000`만 사용합니다.
- UI/레이아웃/스타일 수정 뒤에는 `npm run style:check`와 `npm run build`로 확인합니다.
- 기존 프로젝트 전환은 리디자인이 아니라 구조 이식입니다. 기존 UI, 스타일, 레이아웃, 카피, 프론트 상호작용을 보존합니다.
- 데이터를 표시하는 화면은 데이터를 컴포넌트/페이지에 하드코딩하지 않습니다. 항상 `/api/*` route를 거칩니다(인라인 목 배열 금지). 공식 API 문서가 없어도 `/api/*`에 타입이 붙은 목 route를 만들고, 계약 타입은 `src/lib/api/contracts.ts`에 두어 route와 화면이 공유합니다. 목 route는 실연동이 아니므로 "문서 없을 때 실제 API 연동 금지"와 충돌하지 않습니다.
- 데이터가 새로고침 후에도 남아야 하면(저장/수정/삭제·쌓이는 목록·관리자 데이터·사용자별 데이터) 목에 그치지 말고 개발/검토용 Supabase Postgres로 실제 저장까지 만듭니다. 단 계약(`contracts.ts`)에 있는 필드·route만, 추측 필드·불필요한 테이블/endpoint 없이 **최소로**, 만들기 전 한 줄로 알립니다. 표시만 하는 화면엔 DB를 안 만들고, 애매하면 한 줄 묻습니다. 운영 DB·실서버·secret은 개발자 몫입니다. 자세한 기준은 `docs/SUPABASE_WORKFLOW.md`.
- 프론트엔드 품질: 새 화면은 코드보다 디자인 의도(팔레트·서체·시그니처)를 먼저 한 문단으로 선언하고, 그걸 토큰(`globals.css`·`tailwind.config.ts`, 색은 HSL 성분값)에 박은 뒤, 모든 화면을 그 토큰 + `src/components/ui`로만 만듭니다. 인라인 색상(`text-white`·`bg-slate-100`·`bg-[#...]`)은 `npm run style:check`가 실패시킵니다. 표면은 세 층(`background`→`canvas`→`surface`), 층은 투명도로(`border-border/60`), 그림자는 브랜드 색 토큰으로(`shadow-panel`·`shadow-elevated`). 다크모드는 요청받지 않으면 만들지 않고, 모션은 시그니처 하나에만. 화려함은 표면 중요도에 맞춤(랜딩 과감·표/폼 관습적). UI는 로컬에서 렌더·콘솔을 보고 고칩니다. 브라우저만으로 완결되는 기능(차트·드래그·필터·간단한 편집 등)은 표준·검증된 라이브러리로 필요한 범위까지 구현하되 선택 기준(스택 우선·딱 하나·표준만·검증이 부족하거나 유지 상태가 불분명하면 제외·깔기 전 한 줄·안 되면 철회)을 지킵니다. 단 파일/PDF 내보내기 등은 간단하면 클라이언트지만 무거운 처리(대용량·정밀 출력·보안)는 서버/개발자 몫일 수 있습니다(개발자 이관은 `HANDOFF.md`). 자세한 기준은 `AI_START_HERE.md`의 '프론트엔드 품질 기준'.
- 이미 개발 중인 화면/컴포넌트가 있는 프로젝트가 starter 미적용으로 감지되어도 전체 구조 전환을 자동 실행하지 않습니다. 멈추고 `개발자 확인 필요`로 보고하며, 로컬 규칙 문서와 `scripts/`·설정 기준만 보완하고 `src/app`·`components`·`features` 구조와 화면은 건드리지 않습니다.
- 새 프로젝트/화면/기능 요청은 바로 대량 구현하지 말고 질문형 기획 브레인스토밍을 먼저 합니다(내부 절차, 새 커맨드 아님). **질문이 먼저입니다(하드 게이트): 한 번에 질문 하나씩 묻고, 요구사항이 확정되기 전에는 스펙·계약·코드를 만들지 않습니다. 질문 없이 곧장 계획서·구현으로 넘어가지 않습니다(Codex·Claude Code 공통).** 단, 프로젝트에 기획문서(md)나 만들다 만 화면이 있으면 먼저 읽고 그것을 기준으로 삼아 빠지거나 애매한 부분만 확인합니다(백지에서 다시 묻지 않고, 만들던 화면은 보존). 산출물은 짧은 스펙 + `src/lib/api/contracts.ts` 계약 초안 + `HANDOFF.md` 개발자 이관 목록. 절차는 `docs/PLANNING_ELICITATION.md`.
- schoolp 기존 도메인(주문/디자인/견적/상담 등)과 관련된 기획이면 `docs/domain/`(특히 02·07)을 먼저 참고합니다. as-is 참고자료이며 API 계약이 아니므로 레거시 엔드포인트·상태값을 실연동 대상으로 삼지 않고 목 계약 우선 원칙을 유지합니다. 신규 서비스의 화면 흐름, `/api/*` 목 계약, Supabase migration/seed는 해당 서비스 요구사항으로 새로 설계하며, 기존 DB 테이블·컬럼·enum을 그대로 복사하지 않습니다. 무관한 프로젝트면 무시합니다.
- 협업은 계약 우선(`docs/COLLABORATION_MODEL.md`)입니다. 계약 앞쪽(목 route+화면)은 팀원+AI, 뒤쪽(실 백엔드+DB)은 개발자. 개발자는 ② 계약 확인, ④ 목→진짜 스위치 두 지점만 관여하고, 위험 항목은 멈추지 말고 개발자 이관 목록에 배치로 모읍니다.
- `작업 저장해줘`와 `최신 작업 불러와줘` 절차 안에서만 `git`과 `gh`를 사용합니다. `저장해줘`만 입력되면 먼저 확인 질문을 하며, 저장소는 반드시 private입니다.
- 지정된 GitHub 저장/불러오기 절차 밖의 git clone, commit, push, branch, merge, remote 설정은 금지합니다.
- `배포해줘`는 Vercel production alias를 팀 공유용 화면 검토 링크로 갱신하는 요청입니다. Vercel 배포를 사용자 공개 반영이나 서비스 오픈으로 표현하지 않습니다.
- 인증, 권한, 결제, 개인정보, 운영 DB 설계·전환, secret/운영 환경변수, 공식 도메인, 실제 외부 API 연동, 사용자 공개 반영은 개발자 확인 없이 진행하지 않습니다. 개발/검토용 Supabase Postgres는 `docs/SUPABASE_WORKFLOW.md` 기준으로 진행합니다.

문서가 충돌하면 `AI_START_HERE.md`를 우선하고, 더 제한적인 안전 규칙을 따릅니다.

- `커맨드 목록 보여줘`는 숨겨진 도움말 요청입니다. 이 경우에도 공식 요청 표만 보여줍니다.
