# Browser Smoke Test

- 제품 제목이 RC2인지 확인
- `window.ACDL_RELEASE.version` 확인
- 첫 실행에서 `acdl.calendarTypeDefinitions.v37` 생성 확인
- 탁상형 `sizes.length === 3`
- 엽서형 `sizes.length === 3`
- 달력 유형 관리 닫기 후 `designerHome` 표시 확인
- 시작 템플릿 라벨 확인
