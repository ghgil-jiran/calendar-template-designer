# AI Start Here

이 프로젝트에서 작업하는 AI는 먼저 아래 파일을 읽고 따른다.

`AI_START_HERE.md`가 이 프로젝트의 AI 작업 정본이다. `AGENTS.md`와 `CLAUDE.md`는 각 도구가 이 파일을 찾기 위한 얇은 안내 파일이다.

읽기 순서와 우선순위:

1. `AI_START_HERE.md`
2. `STARTER_VERSION.md`
3. `CHANGELOG.md`
4. `AI_COMMANDS.md`
5. `WORK_LOG.md`
6. `docs/UPDATE_POLICY.md`
7. `docs/UPDATE_APPLY_GUIDE.md`
8. `docs/GITHUB_WORKFLOW.md`
9. `docs/VERCEL_DEPLOYMENT.md`
10. `docs/SCHOOLP_STARTER_GUIDE.md`
11. `docs/COLLABORATION_MODEL.md`
12. `docs/PLANNING_ELICITATION.md`
13. `docs/SUPABASE_WORKFLOW.md`
14. `schoolp_ai_project_guide.md`
15. `docs/API_CONTRACT.md`
16. `README.md`
17. `SANDBOX_WORKFLOW.md`
18. `AGENTS.md` 또는 `CLAUDE.md`

schoolp 기존 도메인(주문/디자인/견적/상담 등)과 관련된 프로젝트라면, 위 문서 외에 `docs/domain/`(schoolp 레거시 코드 분석 as-is 참고자료)도 기획·설계 전에 읽는다. 특히 `docs/domain/02-domain-model.md`와 `docs/domain/07-db-schema.md`(실측 DB 스키마)를 먼저 본다. 단 이것은 도메인 참고 지식이며 **API 계약이 아니다** — 레거시 엔드포인트·필드·상태값을 실연동 대상으로 삼지 않고, 목 계약 우선 원칙을 그대로 적용한다. 새 서비스의 화면 흐름, `/api/*` 목 계약, Supabase Postgres migration/seed는 그 서비스 요구사항으로 새로 설계하며, 기존 DB 테이블·컬럼·enum을 그대로 복사하거나 새 서비스를 레거시 구조에 맞춰 축소하지 않는다. schoolp 주문/디자인 도메인과 무관한 프로젝트라면 `docs/domain/`은 무시한다.

문서 내용이 충돌하면 더 제한적인 규칙을 따른다.

팀원 프로젝트의 `README.md`는 starter 사용법을 길게 설명하는 문서가 아니라 실제 서비스 개요, 주요 화면, 실행 방법, 개발자 확인 필요 항목을 정리하는 문서로 유지한다. starter 사용법과 AI 작업 기준은 `docs/SCHOOLP_STARTER_GUIDE.md`, `AI_START_HERE.md`, `AI_COMMANDS.md`를 기준으로 확인한다.

이미 schoolp 기준을 적용한 기존 프로젝트는 `STARTER_VERSION.md`, `CHANGELOG.md`, 다운로드 페이지를 확인하되, 업데이트 내역만 기준으로 판단하지 않는다. 전체 구조와 현재 기준 문서를 다시 읽고 현재 프로젝트에 부족한 기준만 반영한다. 사용자가 명시적으로 요청하지 않으면 전체 구조 전환을 다시 수행하지 않는다.
starter 적용 여부가 불확실하면 먼저 `.schoolp/starter-state.json`, `STARTER_VERSION.md`, `AI_START_HERE.md`, `starter-manifest.json` 존재 여부와 package scripts를 확인한 뒤 판단한다.

schoolp 안내 페이지 URL만 받은 경우, 그 페이지는 요약본이며 정본이 아니다. 페이지 내용을 요약하는 데서 멈추지 말고 먼저 `schoolp-nextjs-app.zip`을 받아 정본 문서(`AI_START_HERE.md`, `STARTER_VERSION.md`, `CHANGELOG.md`, `AI_COMMANDS.md`, `starter-manifest.json`)를 직접 읽는다. 그다음 `starter-manifest.json`의 `requiredDocs`를 현재 프로젝트 파일과 대조해 부족한 문서/스크립트/기준만 반영한다. 페이지 요약만으로 기준을 적용하지 않는다.

## 팀원 대상 커뮤니케이션 규칙

답변은 팀원이 바로 이해하고 다음에 뭘 할지 알 수 있게 쉬운 말로 쓴다.

- **결과와 다음 할 일부터** 말한다. 무엇이 됐는지, 팀원이 지금 뭘 하면 되는지를 먼저 쓰고 세부는 뒤에 둔다.
- **어려운 용어를 쓰지 않는다.** 에이전트, 워크플로우, 캐시, 훅, 파이프라인, FK/조인, 모델명(GBM 등), P0/P1, 백필, 특징추출 같은 용어는 피하거나, 꼭 필요하면 한 번 쉬운 말로 풀어 준다.
- **AI 내부 동작을 중계하지 않는다.** "백그라운드에서 돌린다", "캐시로 복구한다", "분석 에이전트가", "Analyze/Synthesize 단계" 같은 내부 처리 과정은 팀원에게 설명하지 않는다. 팀원에게 의미 있는 결과만 말한다.
- **호칭을 임의로 붙이지 않는다.** 없는 존칭을 만들어 쓰지 말고 담백한 존댓말을 쓴다.
- **확인이 필요한 항목도 부드럽게** 전한다. 개발자와 맞춰야 하는 항목은 계속 모으되(→ `HANDOFF.md`), "개발자 결정 필요" 같은 딱딱한 라벨 대신 "이건 개발자와 한 번 맞춰야 하는 부분이에요"처럼 서비스 언어로 쓴다. 모으는 것은 유지하고 표현만 바꾼다.

## Starter 업데이트 규칙

- 세션 시작 시 SessionStart 훅이 자동으로 최신 기준을 확인한다. 오래됐으면 AI는 **첫 응답 맨 앞에서, 팀원의 원래 요청을 처리하기 전에 예/아니오로 먼저 묻는다**("스타터 업데이트가 있어요(rXX→rYY). 지금 반영할까요? 1) 네 2) 나중에"). "네"면 아래 안전 반영을 한 뒤 원래 요청을 이어서 처리하고 무엇을 바꿨는지 한 줄로 보고한다. "나중에"면 반영하지 않고 원래 요청만 처리하며 이번 세션엔 다시 묻지 않는다.
- 최신 starter 기준은 매 요청마다 확인하지 않는다. 새 작업 세션, 다른 PC/새 Codex 세션에서 이어서 작업할 때, 배포 전, 큰 기능 개발 전, 반복 오류 발생 시, 사용자가 요청한 경우에만 확인한다.
- 최신 기준 확인 절차는 파일을 수정하지 않는다. AI는 필요할 때 `npm run schoolp:update:check`로 현재 기준과 최신 Standard 차이만 보고한다.
- starter 기준 안전 반영은 리디자인이나 기능 수정이 아니다. 기존 UI, 스타일, 레이아웃, 카피, 프론트 기능은 보존한다.
- starter ZIP을 현재 프로젝트에 통째로 덮어쓰지 않는다. 현재 프로젝트에 부족한 문서, 실행 스크립트, 배포 기준, 안전 규칙만 반영한다.
- `required` 업데이트는 우선 반영하고, `recommended`는 현재 프로젝트에 필요한 경우만 반영한다. `optional`은 요청이 있을 때만 반영하고, `breaking`은 개발자 확인 없이 반영하지 않는다.
- 기존 프로젝트에 반영할 때 슬래시 커맨드가 안 뜨면 스킬 폴더가 안 넘어온 것이다. `.claude/skills/`·`.agents/skills/` 폴더 전체(`schoolp-` 접두어 스킬들)와 `.claude/settings.json`·`.codex/hooks.json`·`scripts/schoolp-*.mjs`를 복사하고 Claude Code 세션을 재시작한다(그래야 `/schoolp-*`가 목록에 뜬다). 자세한 절차는 `docs/UPDATE_APPLY_GUIDE.md`.
- 적용 가능 범위와 금지 범위는 `docs/UPDATE_POLICY.md`, `docs/UPDATE_APPLY_GUIDE.md`, `starter-manifest.json` 기준을 따른다.

## 작업 시작 규칙

팀원이 짧게 요청해도 AI는 먼저 위험 영역을 판단한다.

새 세션의 첫 응답이거나 사용자가 무엇을 할 수 있는지 물어보면 아래 표만 보여준다. 팀원에게는 표의 한국어 문장을 그대로 말하도록 안내한다. Claude Code·Codex에서 동일하게 동작하고, 같은 요청을 `/schoolp-*` 슬래시로도 실행할 수 있다. 저장/불러오기는 GitHub 기준이다. 자세한 기준은 `AI_COMMANDS.md`.

| 하고 싶은 일 | 이렇게 말하세요 |
|---|---|
| 로컬에서 화면 보기 | "실행해줘" |
| GitHub에 작업 저장하기 | "작업 저장해줘" |
| GitHub에서 불러오기 | "작업 불러와줘" |
| 팀 공유용으로 배포하기 | "배포해줘" |
| 핸드오프 정리하기 | "핸드오프 정리해줘" |
| 간단한 사용 안내 보기 | "도움말" |

표 아래에는 세 가지만 짧게 안내한다: 새 화면·기능은 만들 내용을 그대로 설명하면 되고, 막히거나 궁금한 점은 상황을 그대로 질문하면 되며, 점검·최신 기준 확인은 필요한 시점에 AI가 처리하고 Supabase 데이터 확인은 요청하면 된다. 표에는 슬래시를 노출하지 말고 "슬래시로도 됩니다" 정도만 덧붙인다. 슬래시 매핑(AI 참고용): 실행=`/schoolp-run` · 작업 저장=`/schoolp-save` · 불러오기=`/schoolp-load` · 배포=`/schoolp-deploy` · 핸드오프=`/schoolp-handoff` · 도움말=`/schoolp-help`.

이 6개가 팀원에게 노출하는 공식 요청이다. 나머지는 표에 넣지 말고, 필요할 때 말로 부르거나 AI가 알아서 처리한다:

- **기획 브레인스토밍**(`/schoolp-brainstorm`) — 팀원이 새 프로젝트·화면·기능을 설명하면 AI가 자동으로 요구사항 정리부터 시작한다. 명령으로 표에 넣지 않는다(직접 부르려면 "기획해줘").
- **점검**("점검해줘" / `/schoolp-verify`) — 보통 저장·배포 안에서 자동으로 실행된다.
- **최신 기준 확인**("최신 기준 확인해줘" / `/schoolp-update`) — 세션 시작 훅이 자동으로 감지한다.
- **Supabase 열기**("Supabase 열어줘" / `/schoolp-supabase`) — DB 데이터를 직접 볼 때만.
- **이어하기 메모**(`WORK_CONTEXT.md`) — "작업 저장해줘" 때 자동으로 갱신되고 "불러와줘" 때 자동으로 읽어 브리핑한다. 따로 부를 일은 드물다.

GitHub 저장은 회사/집/다른 PC에서 이어서 작업하기 위한 것이다.
Vercel production alias는 팀 공유용 화면 검토 링크다. AI는 Vercel 배포를 사용자 공개 반영이나 서비스 오픈으로 표현하지 않는다.

작업 기록 갱신은 팀원용 커맨드로 노출하지 않고 AI 내부 절차로 처리한다. 사용자가 `도움말`·`커맨드 목록 보여줘`라고 말하면 위 표를 다시 보여준다. 이 커맨드들은 `schoolp-` 접두어 스킬로 등록되어 있어, 한국어 문장으로 말하거나 `/schoolp-*` 슬래시로 부르면 AI가 해당 스킬을 실행한다(Claude Code·Codex 공통).

`저장해줘`만 단독으로 입력되면 의미가 애매하므로 바로 GitHub 저장, 문서 저장, 파일 수정을 진행하지 않는다. `GitHub에 저장하려면 작업 저장해줘라고 말해 주세요. 지금 GitHub에 저장할까요?`처럼 짧게 확인한다.

```text
작업 유형:
위험 영역 포함 여부:
바로 진행 가능한 범위:
개발자 확인 필요 항목:
```

## 로컬 실행 규칙

- 이 프로젝트의 로컬 주소는 항상 `http://localhost:3000`이다.
- `npm run dev`는 3000번 포트로만 실행한다.
- `npm run dev`와 `npm run build`는 시작 전에 `npm run style:check`를 실행한다. 이 검사가 실패하면 스타일 보호 파일을 복구한 뒤 다시 실행한다.
- 3000번 포트가 사용 중이면 다른 포트로 우회 실행하지 말고 기존 dev server를 종료한 뒤 다시 실행한다.
- Windows에서 Node.js 또는 npm을 찾지 못하면 Node.js LTS 설치가 필요하다고 안내하고, 설치 후 Codex/PowerShell을 다시 열게 한다.
- Windows에서는 `start-windows.cmd`를 먼저 실행한다. 이 파일은 Node/npm 확인, 설치 안내, 3000번 포트 정리, 의존성 설치, 로컬 실행을 순서대로 진행한다.
- 실행 후 팀원에게는 `http://localhost:3000` 주소만 안내한다.
- Next dev server 산출물이 꼬인 것으로 보이면 코드와 스타일 보호 파일을 먼저 확인한 뒤 `npm run cache:clean`, `npm run dev` 순서로 재시작한다.

## 스타일 보호 규칙

아래 파일은 전체 스타일 적용을 보장하는 보호 파일이다.

- `src/app/layout.tsx`
- `src/app/globals.css`
- `tailwind.config.ts`
- `postcss.config.js`

규칙:

- `src/app/layout.tsx`의 `import "./globals.css";`를 삭제하거나 다른 파일로 옮기지 않는다.
- `src/app/globals.css`의 `@tailwind base;`, `@tailwind components;`, `@tailwind utilities;`를 삭제하지 않는다.
- `tailwind.config.ts`의 `content` 범위에서 `src/app`, `src/components`, `src/features`, `src/lib` 경로를 삭제하지 않는다.
- 화면 전체를 새 래퍼로 감쌀 때는 기존 DOM 구조, className, 반응형 레이아웃이 유지되는지 확인한다.
- ref, scroll, focus 처리가 필요하면 가장 좁은 기존 요소에 ref를 붙이고, 화면 전체 구조를 바꾸지 않는다.
- UI, 레이아웃, CSS, Tailwind class, wrapper, ref를 수정한 뒤에는 `npm run style:check`와 `npm run build`를 실행한다.
- 스타일이 전부 사라진 것처럼 보이면 `.next` 캐시를 원인으로 단정하지 말고, 먼저 보호 파일과 최근 JSX 구조 변경을 확인한다.

## GitHub 준비 자동화

`작업 저장해줘` 또는 `최신 작업 불러와줘` 요청에서 GitHub 준비가 안 되어 있어도 그 이유만 말하고 멈추지 않습니다. AI는 가능한 준비 작업을 순서대로 진행합니다.

1. `git --version`으로 Git 설치 여부를 확인합니다.
2. Git 저장소가 아니면 `git init`을 진행합니다.
3. `gh --version`으로 GitHub CLI 설치 여부를 확인합니다.
4. `gh`가 없으면 OS에 맞게 설치를 시도합니다.
   - macOS: Homebrew가 있으면 `brew install gh`를 시도합니다.
   - Windows: `winget`이 있으면 `winget install --id GitHub.cli -e`를 시도합니다.
   - 위 방법이 없거나 관리자 권한이 필요하면 GitHub CLI 공식 설치 페이지를 안내하고, 설치 후 Codex/터미널을 다시 열게 합니다.
5. `gh auth status`로 로그인 상태를 확인합니다.
6. 로그인이 안 되어 있으면 `gh auth login`을 실행하고, 브라우저 로그인/회원가입/2FA는 팀원이 직접 진행하게 안내합니다.
7. 인증이 끝나면 AI가 다시 상태를 확인하고 private 저장소 생성, remote 연결, commit, push를 계속 진행합니다.

회원가입, 브라우저 로그인, 비밀번호 입력, 2FA 입력은 AI가 대신하지 않습니다. 하지만 인증이 필요한 지점까지는 AI가 직접 진행하고, 인증 완료 후에는 저장 절차를 계속합니다.

인증 URL이 표시되면 AI는 가능한 경우 브라우저를 열어 해당 URL로 이동합니다. 동시에 팀원이 직접 열 수 있도록 클릭 가능한 링크와 코드를 함께 보여줍니다.

예시:

```text
GitHub 인증이 필요합니다.
URL: [GitHub 인증 페이지](https://github.com/login/device)
Code: ABCD-1234
브라우저에서 로그인/회원가입/2FA/Authorize를 완료해 주세요. 인증이 끝나면 제가 이어서 진행합니다.
```

브라우저 제어가 불가능하면 URL과 코드를 복사하기 쉽게 위 형식으로 안내합니다. 로그인, 회원가입, 비밀번호 입력, 2FA, 계정 선택, 권한 승인은 팀원이 직접 진행합니다.

## GitHub 저장 규칙

GitHub는 작업 저장과 이어서 작업하기 위한 보관소로만 사용한다.

- 팀원은 Git 명령을 직접 실행하지 않는다.
- AI는 `작업 저장해줘`, `최신 작업 불러와줘`, `저장하고 배포해줘`의 저장 확인 절차 안에서만 `git`과 `gh`를 사용한다. Git 저장소가 아니거나 GitHub CLI가 없어도 실패로 끝내지 않고, 가능한 준비 절차를 진행한다. `저장해줘`만으로는 git이나 gh를 실행하지 않고 먼저 확인 질문을 한다. `작업 저장해줘`를 수행할 때는 저장 전 `WORK_LOG.md`를 현재 작업 맥락으로 갱신한다.
- GitHub push는 사용자 공개 반영이나 서비스 오픈이 아니다.
- 처음 저장하는 프로젝트는 `git init`, GitHub CLI 준비, 로그인 확인 후 private 저장소를 생성한다.
- 저장소는 반드시 private으로 만든다.
- `.env*`, DB 파일, `.vercel`, `.next`, `node_modules`, ZIP, 로그 파일은 GitHub에 저장하지 않는다. 단, `.env.example`은 저장할 수 있다.
- force push, hard reset, branch 삭제, remote 강제 변경은 하지 않는다.
- 저장 후 GitHub 저장소 URL, branch, commit hash를 알려준다.

`최신 작업 불러와줘` 요청은 GitHub CLI 준비와 로그인 상태를 확인한 뒤, 먼저 저장하지 않은 변경사항을 확인한다. 변경사항이 있으면 가져오기 전에 `작업 저장해줘`를 제안한다. 충돌이 있으면 자동 해결하지 않고 상황을 설명한다.

## Vercel 준비 자동화

`배포해줘` 요청에서 Vercel 준비가 안 되어 있어도 그 이유만 말하고 멈추지 않습니다. AI는 가능한 준비 작업을 순서대로 진행합니다.

1. `node --version`, `npm --version`으로 Node/npm 사용 가능 여부를 확인합니다.
2. Vercel CLI가 없으면 우선 `npx vercel` 또는 `npx vercel@latest` 기준으로 진행합니다.
3. Vercel 로그인이 필요하면 CLI가 안내하는 브라우저 인증을 팀원이 직접 진행하게 합니다.
4. Vercel 계정이 없으면 팀원이 브라우저에서 회원가입을 진행하게 안내합니다.
5. project link가 없으면 AI가 `vercel link` 흐름을 진행하고, 선택이 필요한 항목은 팀원에게 짧게 확인합니다.
6. 인증과 연결이 끝나면 AI가 다시 빌드 확인 후 production alias 배포를 계속합니다.

회원가입, 브라우저 로그인, 비밀번호 입력, 2FA 입력은 AI가 대신하지 않습니다. 하지만 CLI 준비, 로그인 시작, project link, 배포 재시도는 AI가 이어서 진행합니다.

인증 URL이 표시되면 AI는 가능한 경우 브라우저를 열어 해당 URL로 이동합니다. 동시에 팀원이 직접 열 수 있도록 클릭 가능한 링크를 함께 보여줍니다.

예시:

```text
Vercel 인증이 필요합니다.
URL: [Vercel 인증 페이지](https://...)
브라우저에서 로그인/회원가입/이메일 인증/2FA를 완료해 주세요. 인증이 끝나면 제가 이어서 배포합니다.
```

브라우저 제어가 불가능하면 URL을 복사하기 쉽게 위 형식으로 안내합니다. 로그인, 회원가입, 비밀번호 입력, 이메일 인증, 2FA, 계정 선택, 권한 승인은 팀원이 직접 진행합니다.

## Vercel 배포 규칙

사용자가 `배포해줘`라고 요청하면 현재 작업을 Vercel에 배포한다.

- Vercel 배포는 화면 확인용이다.
- 이 starter는 Vercel production alias를 팀 공유용 화면 검토 링크로 사용한다. Vercel의 production URL이 만들어져도 사용자 공개 반영이나 서비스 오픈이 아니다.
- 기본 배포 명령은 `npm run deploy`이다. 이 명령은 빌드 확인 후 Vercel production alias로 현재 작업을 배포한다.
- Vercel CLI, 로그인, project link가 없어도 실패로 끝내지 않고, 가능한 준비 절차를 진행한다.
- 첫 배포 또는 프로젝트 연결이 불확실한 경우 AI는 배포 전에 Vercel 로그인 계정, team, project를 확인하고 팀원에게 짧게 보고한다.
- 빌드 오류가 있으면 다른 플랫폼으로 우회하지 말고 오류를 수정한 뒤 다시 Vercel 배포를 시도한다.
- 처음 배포하는 PC에서 Vercel 로그인 또는 프로젝트 연결 안내가 나오면 AI가 로그인 절차를 시작하고 팀원이 직접 인증하게 안내한다. 브라우저가 자동으로 열리면 본인 Vercel 계정으로 로그인하고, 브라우저가 열리지 않으면 터미널에 표시된 로그인 URL을 직접 브라우저에 붙여넣어 인증하게 한다. 회원가입, 브라우저 로그인, 비밀번호 입력, 2FA 입력은 팀원이 직접 진행한다. 인증이 끝나면 AI가 배포를 계속한다.
- Netlify, Firebase, Replit, Codex Sites 등 다른 플랫폼을 사용하지 않는다.
- 공식 도메인 연결, 운영 환경변수 설정, DB 전환, 인증/결제/개인정보 연동, 사용자 공개 반영은 개발자 확인 후 진행한다.
- 배포가 끝나면 접속 URL을 알려준다.
- 저장하지 않고 배포한 경우 완료 보고에 `GitHub 미저장 배포`, Vercel URL, 마지막 Git commit 또는 `없음`, 빌드 확인 시각을 함께 적는다.
- Codex가 네트워크 접근, Vercel CLI 실행, 샌드박스 밖 파일 쓰기 승인을 요청하면 이는 로컬 실행 보안 승인이다. Vercel 프로젝트 설정 문제나 시작 파일 제한으로 설명하지 않는다.

배포 전 저장 여부:

- 저장하지 않은 변경사항이 있으면 먼저 아래처럼 짧게 묻는다.

```text
배포 전에 현재 작업을 GitHub에 저장할까요?

1. 저장 후 배포하기 (추천)
2. 배포만 하기
```

- 사용자가 `저장하고 배포해줘`라고 말하면 질문 없이 저장 후 배포한다.
- 사용자가 `배포만 해줘`라고 말하면 저장하지 않고 배포만 한다.
- 저장하지 않고 배포한 경우, 완료 보고에 `GitHub 미저장 배포`라고 표시한다.

## 기존 프로젝트 이식 규칙

기존 프로젝트에서 이 시작 파일을 적용하는 경우, 먼저 이미 schoolp starter가 적용된 프로젝트인지 확인한다. 이미 적용되어 있으면 전체 구조 전환을 다시 하지 않고 부족한 최신 기준만 반영한다. 미적용 프로젝트라면 구조는 schoolp 기준으로 맞추되 기존 UI, 스타일, 레이아웃, 카피, 프론트 상호작용은 원본 수준으로 보존한다.

### 이미 개발 중인 프로젝트 보호 (WIP guard)

starter 미적용으로 감지되더라도, 이미 개발이 진행 중인 프로젝트는 자동으로 전체 구조 전환을 실행하지 않는다.

- 작업 중인 화면/컴포넌트가 이미 존재하는 프로젝트(예: `src/app`, `src/components`, `src/features`, `src/pages`, `app/`, `components/` 등에 실제 개발된 화면이 있는 경우)가 starter 미적용으로 감지되면, 구조를 새로 만들고 화면을 이식하는 전체 전환을 자동으로 실행하지 않는다. 이 경우 작업을 멈추고 `개발자 확인 필요`로 보고한다.
- 이런 프로젝트에는 로컬 규칙 문서(`AI_START_HERE.md`, `AGENTS.md`, `CLAUDE.md`, `docs/*` 등)와 `scripts/`·설정 기준(package scripts, `eslint.config.mjs`, `.gitignore` 등)만 보완한다. `src/app`·`src/components`·`src/features` 구조와 실제 화면은 건드리지 않는다.
- 전체 구조 전환(새 폴더 생성 + 화면 이식)은 사용자가 명시적으로 요청했을 때만 진행한다.
- HTML/MD/정적 목업만 있고 실제 개발된 화면 코드가 없는 프로젝트는 기존 이식 규칙(아래)을 그대로 따른다. 이 보호 규칙은 "이미 개발 중인 화면/컴포넌트가 존재하는 프로젝트"에만 적용한다.

- 다운로드/안내 페이지는 프로젝트 구조와 개발 규칙 안내로만 사용한다.
- 기존 프로젝트 전환은 리디자인이 아니라 구조 이식 작업이다. 기본 화면은 원본 메인 화면과 동일해야 하며, 원본에 없던 UI는 실제 앱 화면에 추가하지 않는다.
- 전환 결과물 안에 보존용 `legacy`, `backup`, `old` 폴더를 임의로 만들지 않는다.
- 기존 HTML/MD/static mockup은 최종 Next.js 화면과 문서에 필요한 내용만 이식하고, 중복 원본 파일은 결과물에 남기지 않는다.
- 기존 화면의 정보 구조, CTA, 입력 흐름, mock 상호작용을 새 구조로 이식한다.
- HTML/MD 정적 목업과 화면 중심 프로젝트도 이 Next.js 시작 파일을 사용한다.
- 사용자가 별도로 요청하기 전까지 포팅 과정에서 디자인을 재구성하지 않는다.
- 반응형 수정은 기존 UI를 보존하거나 깨짐을 고치는 범위에서만 진행한다.
- 기존 파일만 조금 수정하거나 메모 파일만 추가하고 완료하지 않는다.
- 데이터가 남아야 하는 기능은 목으로만 두지 않고 개발/검토용 Supabase Postgres로 실제 저장·조회되게 만든다. 언제/어떻게 만드는지는 아래 "Supabase 저장 규칙"과 `docs/SUPABASE_WORKFLOW.md`를 따른다(필요할 때만, 계약에 묶어 최소만).
- 기본 템플릿에는 활성 Supabase 프로젝트나 DB 연결 코드가 없다. 저장이 필요하면 `docs/SUPABASE_WORKFLOW.md`와 `examples/supabase-prototype-db`를 참고해 필요한 migration/seed/env/API route만 만든다. 운영 DB 설계·전환은 개발자 몫이다.

위험 주제를 다루는 mock UI 시안은 만들 수 있다.

실제 판정, 실제 외부 연동, 실제 시스템 상태 변경, secret 처리, 공식 도메인, 운영 환경변수 설정, 운영 DB 설계·전환, 사용자 공개 반영이 포함되면 구현하지 않고 `개발자 확인 필요`로 멈춘다. 개발/검토용 Supabase Postgres 저장은 여기 해당하지 않으며 아래 "Supabase 저장 규칙"과 `docs/SUPABASE_WORKFLOW.md`에 따라 만든다.

기존 `.env.example`을 `.env.local`로 복사하는 것과 개발/검토용 Supabase migration/seed 실행은 허용된다. `.env.example`에는 필요한 key 이름을 추가할 수 있지만 실제 secret 값은 비워 둔다. key/secret/client 정보는 개발자에게 별도로 전달받아야 하며, 운영 DB 전환은 개발자 확인이 필요하다.

위험 영역:

- 인증
- 권한
- 결제
- 개인정보
- 운영 DB 설계·전환
- 운영 환경변수 및 secret 값
- 새 패키지
- 실제 API 연동
- GitHub public 저장소 생성
- 지정된 GitHub 저장/불러오기 절차 밖의 git clone, commit, push, branch, merge, remote 설정
- Git force push, hard reset, branch 삭제
- Docker/서버 설정
- 공식 도메인, 운영 환경변수, DB 전환, 사용자 공개 반영이 포함된 배포 설정

## 금지

- 지정된 GitHub 저장/불러오기 절차 밖의 git clone, commit, push, branch, merge, remote 설정
- FTP, SFTP, rsync, scp 등 전송 작업
- Docker, CI/CD, nginx, 서버 배포 파일 수정
- Vercel 외 배포 플랫폼 사용
- API 문서에 없는 endpoint, field, status 생성
- secret, token, password, DB URL 하드코딩
- package.json dependency 임의 추가

## 기획 끌어내기 (요구사항 브레인스토밍)

팀원이 새 프로젝트나 새 화면/기능을 요청하면, AI는 바로 대량 구현으로 들어가지 않고 먼저 질문형 브레인스토밍으로 기획을 함께 끌어낸다.

- **질문이 먼저다 (하드 게이트).** 한 번에 질문 하나씩 묻고, 요구사항이 확정되기 전에는 스펙·`contracts.ts` 계약·코드를 만들지 않는다. 질문 없이 곧장 계획서·구현으로 넘어가는 것이 가장 흔한 실패다. 답이 모호하면 보기를 제시해 확정될 때까지 되묻고, 모순되는 가정은 짚어서 되묻는다. Codex·Claude Code 공통. 자세한 규칙은 `docs/PLANNING_ELICITATION.md`의 "절대 규칙 — 하드 게이트".
- **이미 기획문서가 있으면 먼저 읽는다.** 프로젝트에 기획문서(md 등)나 만들다 만 화면이 있으면 그것을 기획 기준(plan of record)으로 삼고, 백지에서 다시 묻지 않는다. 빠지거나 애매하거나 서로 안 맞는 부분만 확인하고, 만들던 화면은 보존한 채 이어서 구현한다. 그런 자료가 없을 때만 아래 질문 절차를 처음부터 진행한다.
- 아키텍처가 아니라 **서비스 언어로** 묻는다: 무슨 서비스인지, 누가 쓰는지, 핵심 화면과 각 화면의 데이터.
- 팀원이 놓치기 쉬운 것을 대신 캐낸다: 빈 상태/오류 상태, 권한, 저장·수정·삭제 필요 여부, 검색·필터, 개인정보·결제·인증과 닿는 부분.
- 산출물 3가지를 반드시 만든다: (1) 짧은 스펙, (2) `src/lib/api/contracts.ts`의 **데이터 모양(계약) 초안**, (3) `HANDOFF.md`의 **개발자 이관 목록**(위험 항목을 배치로 모음).
- 이 절차는 새 프로젝트·화면·기능 요청이 들어오면 AI가 **자동으로 먼저 시작**한다. 동시에 팀원이 `기획해줘` 또는 `/schoolp-brainstorm`으로 직접 부를 수도 있다(스킬로 등록됨).
- schoolp 기존 도메인(주문/디자인/견적/상담 등)과 관련된 기획이면, 질문을 시작하기 전에 `docs/domain/`(특히 02·07)을 먼저 읽어 현재 시스템이 어떻게 되어 있는지 파악하고 서비스 언어로 묻는다. 이 문서는 as-is 참고자료이며 계약이 아니므로, 계약 초안은 스타터 규칙대로 새로 설계한다. 기존 DB 스키마는 용어와 연동 지점 확인용이며, 새 서비스 DB나 API 설계의 강제 템플릿이 아니다.

자세한 절차는 `docs/PLANNING_ELICITATION.md`, 이 산출물이 이어지는 협업 흐름은 `docs/COLLABORATION_MODEL.md`를 따른다.

## 범위 규율 (과잉개발 방지)

AI로 개발할 때 가장 흔한 실패는 **시키지 않은 것을 과하게 만드는 것**이다. 요청한 범위만 만든다.

- **안 얹는다:** 안 시킨 화면·기능·페이지, 요청 없는 대규모 리팩터·구조 변경·라이브러리 추가, 미래 대비 추상화(지금 안 쓰는 것). 곧 버려질 목업을 테스트나 과한 방어 로직으로 하드닝하지 않는다.
- **그래도 완성도는 줄이지 않는다 (이건 과잉이 아니다):** 화면이 제대로 돌아가는 빈/로딩/오류 상태, 반응형·접근성 기본, 명백한 버그 수정, 워크플로가 요구하는 목 `/api`·계약 타입·필요 시 Supabase 저장은 그대로 만든다.
- **애매하면 묻는다:** 과한지 확실치 않으면 조용히 빼지도, 조용히 얹지도 말고 만들기 전에 한 줄로 알리고 팀원이 자른다.

"범위(안 시킨 것)"와 "완성도(제대로 돌아가는 것)"는 다른 축이다. 막는 것은 범위 초과이고, 완성도는 막지 않는다. 데이터/DB에 대한 구체 기준은 아래 "Supabase 저장 규칙"의 "딱 필요한 만큼만"을 따른다.

## 프론트엔드 품질 기준

화면·기능의 완성도를 매번 안정적으로 끌어올리기 위한 기준이다. 잘 만드는 빌더가 "기본값으로" 예쁘게 뽑는 이유는 스택·디자인 규율·확인 루프를 하네스가 미리 깔아둔 것뿐이며, 아래가 그 역할을 한다.

**1. 디자인 — 독창성은 한 번, 규율은 매 화면.**

- **코드보다 의도를 먼저 쓴다.** 새 화면은 첫 줄을 쓰기 전에 한 문단으로 선언한다 — 팔레트(4~6개), 서체, 이 화면을 기억하게 할 시그니처 하나. 그 다음 토큰으로 옮기고, 그 다음에 화면 코드다. 순서를 뒤집으면 반드시 인라인 색상이 섞인다.
- 디자인 방향(팔레트·타이포·시그니처)은 이 서비스에 맞게 **의도적으로** 고른다. 템플릿틱한 AI 기본값(크림 배경+세리프+테라코타 류)에 기대지 않는다. 방향 잡기는 `frontend-design` 스킬을 따른다.
- 고른 방향은 **토큰에 박는다**: 색·그림자·폰트·반경을 `src/app/globals.css`·`tailwind.config.ts`의 디자인 토큰으로 정의한다. 색은 HSL 성분값(`H S% L%`)으로만 적는다 — `hsl()`로 감싸면 `bg-primary/80` 같은 투명도 수식어가 죽는다.
- 모든 화면은 그 **토큰 + `src/components/ui` 컴포넌트로만** 만든다. `text-white`·`bg-slate-100`·`bg-[#...]` 금지 — `npm run style:check`가 잡아서 **실패시킨다**. 컴포넌트 커스텀은 변형(variant) 추가로 하고, `className`으로 색을 덮어쓰지 않는다.
- 이 검사는 `.schoolp/starter-state.json`에 `"styleContract": "tokens-v1"`이 있을 때만 실패시킨다. **자기 스타일을 이미 가진 기존 프로젝트에는 켜지 않는다** — 켜면 `globals.css`를 고쳐야 통과하는데 그 파일은 `manualReview`라 손댈 수 없어 `dev`·`build`가 막힌다. 팀원이 "디자인 토큰 반영해줘"라고 하면 그때 토큰을 반영하고 켠다. 꺼져 있으면 경고만 출력하고 통과한다.
- **표면은 세 층**(`background` → `canvas` → `surface`)으로 깊이를 만들고 회색을 늘리지 않는다. 층은 새 색이 아니라 **투명도**로 만든다(`border-border/60`, `bg-primary/85`).
- **그림자는 회색이 아니라 브랜드 색이다.** 역할별 토큰을 쓴다 — `shadow-panel`(붙어 있는 면), `shadow-elevated`(떠 있는 것), `shadow-canvas`(주인공 영역).
- **다크모드는 요청받지 않으면 만들지 않는다.** 모션은 상태 변화에만 쓰고, 힘은 시그니처 하나에 몰아준다. 흩뿌린 애니메이션이 "AI가 만든 티"의 주범이다.
- **화려함은 표면 중요도에 맞춘다:** 랜딩·히어로처럼 첫인상 화면은 과감하게, 데이터 표·폼·설정 화면은 관습적이고 깔끔하게. 독창성은 방향에서, 안정감은 실행(토큰 준수)에서 나온다.

**2. 만든 화면은 보고 고친다 (프리뷰 루프).**

- UI를 만들거나 바꾼 뒤에는 "만들고 끝"이 아니라 로컬(`http://localhost:3000`)에서 실제로 렌더해 보고, 브라우저 콘솔 오류를 확인한 뒤 고친다. 스타일/빌드 확인은 `npm run style:check`·`npm run build`.

**3. 브라우저에서 바로 처리할 수 있는 기능은 필요한 범위까지 구현한다 — 단, 서버 처리가 필요한지 먼저 판단한다.**

- **브라우저에서 처리:** 차트·그래프, 드래그 정렬, 검색·필터·정렬(이미 받은 데이터 안에서), 이미지 크롭·미리보기, 간단한 편집 UI, 간단한 계산 등.
- **경계선(판단 필요):** 파일·이미지·PDF 내보내기는 간단하면 클라이언트로 되지만("내보내기 = 무조건 서버 없이"가 아니다), 무거운 처리(대용량 변환·정밀 출력·보안 민감 등)는 서버나 실제 파이프라인(개발자 몫)이 필요할 수 있다. 이런 건 개발자 이관 목록(`HANDOFF.md`)에 남긴다.
- 계약 앞쪽(브라우저 완결)으로 판단되면, 사용자가 요청한 기능은 성숙한 표준 라이브러리로 구현한다.
- **라이브러리 선택 가드레일:**
  - 스택 안(Tailwind·Radix·`src/components/ui`·기존 의존성)에서 되면 새로 깔지 않는다.
  - 요청된 기능의 핵심에만, **딱 하나씩**. 겹치는 라이브러리를 쌓지 않는다.
  - 그 분야의 **표준·검증된 것만** — 사용량 많고 유지보수 활발하고 라이선스 문제없는 것. 무명·방치·네이티브 빌드 필요·보안 이슈 있는 것은 배제. (예시: 캔버스 fabric/konva, PDF pdf-lib/jsPDF, 차트 recharts, 드래그 dnd-kit, 날짜 date-fns — 예시일 뿐, 케이스에 맞는 표준을 고른다.)
  - **검증된 표준을 찾기 어렵거나 기능이 단순하면 무리해서 라이브러리를 추가하지 않는다:** 기본 요소로 직접 만들거나, 선택지가 불분명하다고 알린 뒤 진행 방향을 정한다. 검증이 부족하거나 유지 상태가 불분명한 라이브러리를 억지로 끼워 넣지 않는다.
  - 깔기 전에 한 줄로 알린다("이 기능엔 X를 씁니다 — 이유"). 팀원이 자를 수 있게.
  - 프리뷰 루프로 실제 동작을 확인한다. 프레임워크와 싸우거나 반쯤 엮여 지저분하면 남기지 말고 되돌린다.
  - Next.js에서 캔버스류 클라이언트 전용 라이브러리는 `'use client'` + 필요 시 dynamic import(`ssr: false`)로 쓴다.

## 데이터 표시 규칙 (BFF 기본)

데이터를 표시하는 화면은 데이터를 항상 `/api/*` route를 거쳐서 받는다. 팀원 AI는 화면을 만들 때 BFF 계층을 기본으로 생성한다.

- 데이터를 표시하는 화면은 컴포넌트/페이지에 데이터를 하드코딩하지 않는다. 반드시 `/api/*` route를 거친다. 컴포넌트 안에 인라인 목(mock) 배열을 두는 것을 금지한다.
- 공식 API 문서가 없어도 화면에 데이터가 필요하면, 컴포넌트에 값을 박아 넣지 말고 `/api/*`에 타입이 붙은 목 route를 만든다. 화면은 그 목 route에서 데이터를 받아 목록과 요약 수치를 모두 데이터에서 계산한다.
- 계약(데이터 모양) 타입은 `src/lib/api/contracts.ts`에 두고 route와 화면이 같은 타입을 import 해서 공유한다. 화면 컴포넌트가 데이터 모양을 자체 정의하지 않게 한다.
- 나중에 목 route만 실제 연동으로 교체할 수 있게 seam을 유지한다. 실제 API가 연결되면 화면과 계약 타입은 그대로 두고 목 route 내부(또는 화면의 fetch 지점)만 실제 fetch로 바꾼다.
- 이 규칙은 "문서 없을 때 실제 API 연동 금지" 규칙과 충돌하지 않는다. 목 route는 실제 백엔드 연동이 아니라 화면 개발용 목 데이터일 뿐이다.

기본 템플릿의 `src/app/api/applications/route.ts`, `src/lib/api/contracts.ts`, `src/app/page.tsx`가 이 계약형 구조의 레퍼런스다. 새 화면도 같은 방식(목 route + 공유 계약 타입 + 화면에서 계산)으로 만든다.

단, **이 파일들은 빈 폴더에서 새로 시작할 때의 시작 화면이지 복사해 넣을 자산이 아니다.** 이미 화면이 있는 프로젝트에는 starter의 `src/**`를 복사하지 않는다 — `src/app/page.tsx`가 들어가면 루트 라우트를 예제 화면이 가로챈다. 구조만 참고하고 그 프로젝트의 화면은 그대로 둔다.

계약은 단순한 코드 규칙이 아니라 **팀원과 개발자 사이의 이관 경계**다.

- 계약(데이터 모양)의 **앞쪽**(`/api/*` route + 화면·기능, 그리고 데이터가 남아야 하면 개발/검토용 Supabase Postgres 저장까지)은 팀원 + AI가 만든다.
- 계약의 **뒤쪽**(운영 API + 운영 DB)은 개발자가 만든다. 팀원은 운영 구현을 확정하지 않고, Supabase 구현과 운영 전환 항목을 `HANDOFF.md`의 개발자 이관 목록에 모은다.
- 공식 API가 준비되면 개발자가 `docs/API_CONTRACT.md`에 실제 명세를 채우고, 그때 목 route만 실제 연동으로 교체한다. 계약과 화면은 그대로 두므로 **화면은 바뀌지 않는다.**

이 협업 흐름의 전체 그림과 개발자 확인 시점은 `docs/COLLABORATION_MODEL.md`를 따른다.

## 개발자 확인 사항 (GitHub Issue)

개발자는 프로젝트를 주기적으로 검토하고 방향을 이 저장소의 GitHub Issue로 남긴다. 팀원은 이 경로를 몰라도 되며, AI가 대신 확인하고 반영한다.

**확인 시점** — `node scripts/schoolp-issue-check.mjs`를 실행한다. **작업을 시작하는 순간에만 확인한다.**

- 세션 시작 (SessionStart 훅이 자동 실행)
- `작업 불러와줘` 후 (다른 PC에서 이어받는 시점)

붙이지 않는 곳과 이유:

- `작업 저장해줘` 전 · `배포해줘` 전 — 대부분 퇴근 직전이거나 회의 직전이다. 그 시점에 새 작업을 전달하면 처리할 시간이 없고 부담만 남는다. 저장하고 퇴근한 뒤 다음 세션에서 받으면 된다.
- `실행해줘` — 가장 빈번한 명령이라 매번 네트워크 호출이 붙으면 체감 속도가 나빠진다.

**반드시 fail-open이다.** git 저장소가 아니거나, `gh`가 없거나, 인증이 안 되어 있거나, 네트워크가 끊겨도 조용히 넘어간다. 확인 실패로 팀원 작업을 멈추지 않는다.

**처리 기준**

- 팀원에게 `GitHub`, `Issue`, `저장소` 같은 용어를 쓰지 않는다. "개발자 확인 사항이 왔어요"처럼 서비스 언어로 전한다.
- 기술적 변경(저장소 교체, 의존성 정리 등)은 무엇을 할지 한 줄로 알리고 진행한다. 팀원이 판단할 근거가 없는 것을 묻지 않는다.
- 팀원 눈에 보이는 변화(데이터가 사라짐, 기능이 빠짐, 화면이 달라짐)는 진행 전에 팀원에게 묻는다.
- 반영을 마쳤고 결과를 코드로 확인했으면 `gh issue comment <번호>`로 무엇을 했는지 남기고 `gh issue close <번호>`.
- 일부만 처리했거나 판단이 필요하면 코멘트만 남기고 **닫지 않는다.** 닫힘은 개발자에게 "완료"로 읽히므로 확실할 때만 닫는다.
- 팀원이 새 커맨드를 배우지 않는다. 공식 요청 표(6개)에 추가하지 않는다.

**같은 항목을 다시 작업하지 않는다.** 반영 후 닫지 않은 Issue는 다음 확인 때 다시 나타난다. 조회 결과에는 이전 처리 코멘트가 함께 담기며, `[처리 기록]`이 붙은 항목은 이미 작업된 것이다.

- 기록을 먼저 읽고 **남은 부분만** 처리한다.
- 남은 일이 없으면 아무것도 하지 않고 넘어간다. 코멘트도 추가하지 않는다(같은 내용이 쌓이면 기록을 읽기 어려워진다).
- 그래서 처리 코멘트는 **무엇을 했고 무엇이 남았는지** 알 수 있게 쓴다. 다음 세션의 판단 근거가 이 코멘트뿐이다.

## Supabase 저장 규칙 (프로토타입 DB)

데이터가 실제로 남아야 하는 기능은 목으로만 두지 말고, 개발/검토용 Supabase Postgres에서 실제로 저장·조회되게 만든다. 단 **필요할 때만, 계약에 묶어 최소로만** 만든다. "optional이라 안 만듦"도, "무조건 만듦"도 아니다.

**언제 만드나 (트리거).** 아래 중 하나면 Supabase Postgres migration/seed와 `/api/*` route를 만들어 계약 뒤에 실제로 연결한다.

- 입력한 데이터가 새로고침·재시작 후에도 남아야 함
- 쌓이는 목록(신청/게시글/재고 등), 기존 항목 수정·삭제
- 관리자가 관리하는 데이터, 로그인 사용자별 데이터

**언제 안 만드나.** 표시만 하는 화면·시안·랜딩, 저장하지 않는 일회성 계산/미리보기, 외부에서 올 데이터를 보여주기만 하는 경우 → 목으로 둔다. DB를 만들지 않는다.

**애매하면 묻는다.** 저장이 필요한지 불분명하면 조용히 건너뛰지 말고 팀원에게 한 줄로 묻는다: "이 데이터, 새로고침 후에도 남아 있어야 하나요? 나중에 목록으로 다시 봐야 하나요?" 예/아니오만 받으면 된다.

**딱 필요한 만큼만 (과생성 금지).**

- Supabase schema와 route는 `src/lib/api/contracts.ts`의 합의된 데이터 모양과 1:1로 맞춘다. 어떤 화면도 읽거나 쓰지 않는 필드·테이블·endpoint는 만들지 않는다.
- 추측 필드 금지: `createdAt`/`updatedAt`, soft-delete, 권한, 감사로그, 페이지네이션 등은 그 기능이 명시적으로 요구할 때만 넣는다.
- 지금 그 기능이 쓰는 최소만 만든다. 앱 전체 데이터 모델을 미리 설계하지 않는다.
- 이미 있는 `/api/*` 목 route의 **내부만** Supabase 읽기/쓰기로 바꾼다. 계약 밖으로 새 endpoint·새 타입을 늘리지 않는다.

**만들기 전에 한 줄로 알린다.** 예: "만들 것: Supabase `applications` 테이블(이름·연락처·메모) + `/api/applications` 저장/조회". 팀원이 과한지 보고 잘라낼 수 있게 한다.

**개발/검토용 Supabase 구현은 진행할 수 있다.** 다만 운영 DB 설계·전환, 실제 외부 API 연동, secret 값, 공식 도메인, 사용자 공개 반영은 개발자 몫이다. Supabase project 선택, login, access token, secret 입력은 팀원이 직접 진행한다.

**개발자에게 넘어가는 것.** Supabase schema, seed, route는 버려지는 게 아니라 **동작하는 요구사항 명세**다. 개발자가 이를 검토해 운영 DB와 운영 API를 설계하고 전환한다. 계약(`contracts.ts` + `/api/*` 모양)은 그대로 이어지고, Supabase schema는 운영 전환 검토용 출발점이다. 관련 항목은 `HANDOFF.md`에 모은다.

Supabase CLI, Auth, Storage, Realtime, env/secret boundary는 `docs/SUPABASE_WORKFLOW.md`를 따른다.

## API 연동

실제 API 연동은 `docs/API_CONTRACT.md`에 공식 API 문서 URL 또는 endpoint 명세가 있을 때만 진행한다. schoolp member API와 print API는 공식 문서가 있으므로 `docs/API_CONTRACT.md`의 schoolp 공식 API 계약을 우선한다.

API 공식 문서가 없으면 실제 endpoint/field/status를 확정하지 않는다. 대신 위 "데이터 표시 규칙"에 따라 `/api/*`에 타입이 붙은 목 route를 만들고, 화면은 그 목 route에서 데이터를 받아 렌더한다. 컴포넌트에 인라인 목 배열을 두지 않는다.

## 빌드 오류 대응

일반 TypeScript, import, CSS 오류는 수정 시도한다.

오류 원인이 인증, 권한, 결제, 개인정보, 운영 DB 전환, 운영 환경변수, 새 패키지, 공식 도메인, 사용자 공개 반영과 관련되거나 같은 오류가 2회 이상 반복되면 `개발자 확인 필요`로 멈춘다.

팀원 sandbox 안에서 코드 수정과 로컬 실행만 수행한다.
